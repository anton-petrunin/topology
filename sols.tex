\chapter{Semisolutions}


\spell{\begin{multicols}{2}}{}


\parbf{\ref{ex:morning}.}
Topologically speaking, a T-shirt is a disc with three holes, two for the hands and one for the head;
pants have two holes, one for each leg.
In an ideal universe socks should not have holes, but apparently our universe is not ideal.

\parbf{\ref{ex:mug}.}
He saw a hole in the bottom, so the topology of the mug is the same as a donut (topologically speaking, a solid torus).

{

\begin{wrapfigure}{r}{25 mm}
\vskip-5mm
\centering
\includegraphics[scale=.25]{pics/5a}\\
\end{wrapfigure}

\parbf{\ref{ex:finger-ring}.}
Draw the hair tie on each picture, the last one is shown.

\parit{Source.} Suggested by Rostislav Matveev.

}

\parbf{\ref{ex:scissors}}; \ref{SHORT.ex:scissors:scissors}.
Push the loop up thru the other handle, bring it
around over the points and back over the handles; the
string will come off.

\parit{\ref{SHORT.ex:scissors:4-hands}.}
Take the center of the string holding your wrists, push it up thru one of the loops on your friend's wrist and bring it down over his hand.

\parit{\ref{SHORT.ex:scissors:2-hands}.}
Use the same trick as in \ref{SHORT.ex:scissors:4-hands}.

\parit{Source.}
Part \ref{SHORT.ex:scissors:scissors} and \ref{SHORT.ex:scissors:4-hands} appear in Harry Houdini's book \cite{houdini}.
A version of part \ref{SHORT.ex:scissors:scissors} appears earlier in the family of Sam Loyd \cite{loyd} under the name ``The Gordian knot''.
Part \ref{SHORT.ex:scissors:4-hands} under the name ``The prisoners' release puzzle'' appears in Cassell's book \cite{cassell} published in 1881.
Both puzzles should be much older.

\parbf{\ref{ex:not-a-metric}.}
Check the triangle inequality for $0$, $\tfrac12$, and~$1$.

\parbf{\ref{ex:metric}.}
Check the conditions in \ref{def:metric-space}.

\parbf{\ref{ex:d1+d2+dinfty}.}
Check all the conditions in Definition~\ref{def:metric-space}.
Further we discuss the triangle inequality --- the remaining conditions are nearly evident.

Let $a=(x_a,y_a)$, $b=(x_b,y_b)$, and $c\z=(x_c,y_c)$.
Set
\begin{align*}
x_1&=x_b-x_a,
&
y_1&=y_b-y_a,
\\
x_2&=x_c-x_b,
&
y_2&=y_c-y_b.
\end{align*}

\parit{(a).}
The inequality
$$|a-c|_1\le |a-b|_1+|b-c|_1$$
can be written as
$$|x_1+x_2|+|y_1+y_2|
\le
|x_1|+|y_1|+|x_2|+|y_2|.$$
The latter follows since $|x_1+x_2|\le |x_1|+|x_2|$
and
$|y_1+y_2|\le |y_1|+|y_2|$.

\parit{(b).}
The inequality
$$|a-c|_2\le |a-b|_2+|b-c|_2\eqlbl{eq:trig-inq-d2}$$
can be written as
\begin{align*}
&\sqrt{\bigl(x_1+x_2\bigr)^2+\bigl(y_1+y_2\bigr)^2}\le
\\
&\qquad\le
\sqrt{x_1^2+y_1^2}+\sqrt{x_2^2+y_2^2}.
\end{align*}
Take the square of the left and the right-hand sides,
simplify,
take the square again and simplify again.
You should get the following inequality:
$$0
\le
(x_1\cdot y_2-x_2\cdot y_1)^2,$$
which is equivalent to \ref{eq:trig-inq-d2}
and evidently true.

\parit{(c).}
The inequality
$$|a-c|_\infty\le |a-b|_\infty+|b-c|_\infty$$
can be written as
$$
\begin{aligned}
&\max\{|x_1+x_2|,|y_1+y_2|\}\le
\\
&\qquad\le
\max\{|x_1|,|y_1|\}+\max\{|x_2|,|y_2|\}.
\end{aligned}
\eqlbl{eq:max-trig}$$
Without loss of generality, we may assume that
$$\max\{|x_1+x_2|,|y_1+y_2|\}=|x_1+x_2|.$$
Further,
\begin{align*}
|x_1+x_2|&\le |x_1|+|x_2|\le
\\
&\le\max\{|x_1|,|y_1|\}+\max\{|x_2|,|y_2|\}.
\end{align*}
Hence \ref{eq:max-trig} follows.

\parbf{\ref{ex:dist-cont}.}
Show that the triangle inequality implies that
$|f(x)-f(y)|<\eps$
if
$|x-y|_{\c{X}}<\eps$;
make a conclusion.

\parbf{\ref{ex:comp+cont}.}
Fix $x\in \c{X}$ and $y\in\c{Y}$
such that $f(x)=y$.

Fix $\epsilon>0$.
Since $g$ is continuous at $y$, there is a positive value $\delta_1$ such that 
$$|g(y')-g(y)|_{\c{Z}}<\epsilon
\quad
\text{if}
\quad
|y'-y|_{\c{Y}}<\delta_1.$$

Since $f$ is continuous at $x$, there is $\delta_2>0$ such that 
$$|f(x'-f(x)|_{\c{Y}}<\delta_1
\quad
\text{if}
\quad
|x'-x|_{\c{X}}<\delta_2.$$

Since $f(x)=y$, we get that
$$|h(x')-h(x)|_{\c{Z}}<\epsilon
\quad
\text{if}
\quad
|x'-x|_{\c{X}}<\delta_2.$$
Hence the result.

\parbf{\ref{ex:isom-cont}.} \ref{SHORT.ex:isom-cont:continuous}
Show that the triangle inequality implies that
$|f(x)-f(y)|_{\c{Y}}<\eps$
if
$|x-y|_{\c{X}}<\eps$;
make a conclusion.

\parit{\ref{SHORT.ex:isom-cont:injective}.}
Apply \ref{def:metric-space:b}.

\parbf{\ref{ex:to-descrete}.} Show and use that in \ref{def:cont-epsilon-delta} one can take $\delta=1$ for any $\eps>0$.

\parbf{\ref{ex:infinite-inverse-image}.}
Learn about space-filling curves and think.
Alternatively, guess the patern in in the following graphs and pass to the limit.

\begin{Figure}
\vskip-0mm
\centering
\includegraphics{mppics/pic-150}
\vskip-0mm
\end{Figure}


\parbf{\ref{ex:d1+d2+dinfty-balls}.}
Figure out which is which.

\begin{Figure}
\vskip-0mm
\centering
\includegraphics{mppics/pic-60}
\vskip-0mm
\end{Figure}

\parbf{\ref{ex:r<2R}.}
Apply the triangle inequality to $x$, $y$, and $z\in \Ball(y,R)\setminus \Ball(x,r)$.
For the second part, consider the balls $\Ball(2,3)$ and $\Ball(0,4)$ in $[0,\infty)$.

\parit{Comment.} Note that we used that $\Ball(y,R)\z\ne \Ball(x,r)$,
without this condition there are no general restrictions on $r$ in terms of $R$.
For example the inclusion $\Ball(x,1000)\subset\Ball(y,1)$ holds in the discrete space.

\parbf{\ref{ex:def:cont-epsilon-delta=def:cont-balls}.}
Spell the definitions.

\parbf{\ref{ex:ball-is-open}.}
If $y\in \Ball(x,R)$, then $r=R-|x-y|\z>0$.
Use the triangle inequality to show that $\Ball(y,r)\z\subset \Ball(x,R)$.
Make a conclusion.

\parbf{\ref{ex:open-union}.}
Apply \ref{def:open}.

\parbf{\ref{ex:open-intersection}.} Apply \ref{def:open}.

\parbf{\ref{ex:union-of-balls}.} The if part follows from \ref{ex:ball-is-open} and \ref{ex:open-union}.
It remains to prove the only-if part.

Let $V$ be an open set.
By \ref{def:open}, for any $x\in V$ there is $r_x>0$ such that $\Ball(x,r_x)\subset V$.
Observe that
\[V=\bigcup_{x\in V} \Ball(x,r_x).\]

\parbf{\ref{ex:infty-open-intersection}.}
Consider the open segments $(-\eps,\eps)$ for all $\eps>0$ in $\RR$.
Note that 
\[\{0\}=\bigcap_{\eps>0}(-\eps,\eps)\]
and the one-point set $\{0\}$ is not open.

\parbf{\ref{ex:d1+d2+dinfty-open}.}
Show and use that 
\[\Ball(x,r)_1\subset \Ball(x,r)_2\subset \Ball(x,r)_\infty\subset \Ball(x,2\cdot r)_1;\]
here $\Ball(x,r)_1$, $\Ball(x,r)_2$, and $\Ball(x,r)_\infty$ denote the balls in the metrics $|{*}-{*}|_1$, $|{*}-{*}|_2$, and $|{*}-{*}|_\infty$ respectively.

\parbf{\ref{ex:image-of-open}.} 
Look at the image of $\RR$ under the function $x\mapsto |x|$.

\parbf{\ref{ex:unique-lim-metr}.}
Assume the contrary; that is, a sequence $x_1,x_2,\dots$ has two limits $y$ and $z$.
Set $r\z=|y-z|$.
Note that $\Ball(y,\tfrac r2)$ contains all but finitely many elements of the sequence $x_1,x_2,\dots$;
the same holds for $\Ball(z,\tfrac r2)$.
Observe that $\Ball(y,\tfrac r2)\z\cap \Ball(z,\tfrac r2)=\emptyset$ and arrive at a contradiction.

\parbf{\ref{ex:continuous-limit}.}
Suppose $f$ is not continuous.
This means that there is a point $x_\infty$ and $\eps>0$ such that there is a point $x_n\in \Ball(x_\infty,\tfrac1n)$ such that $|f(x_n)-f(x_\infty)|>\eps$.
In particular, $y_n=f(x_n)$ does not converge to $y_\infty=f(x_\infty)$.
It proves the if part of the exercise.

To prove the only-if part, suppose that there is a sequence $x_n\to x_\infty$ such that $y_n\not\to y_\infty$ as $n\to \infty$.
Note that in this case we can pass to a subsequence so that $x_n\in \Ball(x_\infty,\tfrac1n)$ and $|y_n-y_\infty|>\eps$ for some fixed $\eps>0$.
From above, $f$ is not continuous.

\parbf{\ref{ex:nonclosed-nonopen}.}
Show that the semiopen interval $[0,1)$ is neither open nor closed in $\RR$.

\parbf{\ref{ex:closure-is-closed}.}
Choose a point $z\in \bar{\bar A}$.
It means that there is a sequence $y_1,y_2,\dots\in\bar A$ such that $y_n\to z$ as $n\to\infty$.
The latter means that for each $y_i$ there is a sequence $x_{i,1}, x_{i,2},\dots\in A$ such that $x_{i,n}\to y_i$ as $n\to\infty$.
Try to choose a sequence of integers $m_n$ such that $x_{n,m_n}\to z$ as $n\to\infty$.
Make a conclusion.

\parbf{\ref{ex:closed-open-complement}.}
Show that $Q$ is closed if $x\in Q$ if and only if $\Ball(x,\eps)\cap Q\ne\emptyset$ for any $\eps>0$.
Show that the latter is equivalent to $y\in V$ if and only if $\Ball(y,\eps)\subset V$ for some $\eps>0$.
Make a conclusion.

\parbf{\ref{ex:cofinite-metrizable}.}
Let $\c{X}$ be an infinite set with cofinite topology.
Show that any two nonempty open sets in $\c{X}$ have nonempty intersection.
Show that the latter does not hold for open balls in a metric space with at least two points.


\parbf{\ref{ex:finite+metrizable}.}
Let $\c{F}$ be a finite metric space.
Observe that there is $\eps>0$ such that $|x-y|>\eps$ for any two distinct points $x,y\in\c{F}$.
It follows that $\{x\}=\Ball(x,\eps)$ for any $x\in\c{F}$;
in particular, each one-point set is open.
By \ref{def:top-space:u}, any set in $\c{F}$ is open.

\parbf{\ref{ex:weaker-top}.}
Choose $W\in \s{W}$.
By assumption, for any $w\in W$ there is $S_w\in \s{S}$ such that $W\supset S_w\ni w$.
Observe that 
\[W=\bigcup_{w\in W}S_w.\]
It follows that $\s{W}\subset \s{S}$;
in other words, any $\s{W}$-open set is $\s{S}$-open.

\parbf{\ref{ex:connect-two-point-maps}}; \ref{SHORT.ex:connect-two-point-maps:R->X}.
Consider the function defined by
\[
f(x)=
\begin{cases}
\quad a &\text{if}\ x< 0,
\\
\quad b &\text{if}\ x\ge 0.
\end{cases}
\]

\parit{\ref{SHORT.ex:connect-two-point-maps:X->R}.} Suppose $f\:\c{X}\to \RR$ is nonconstant;
that is, $f(a)\ne f(b)$.
Note that $W=\RR\setminus\{f(a)\}$ is an open set containing $b$.
Assume $f$ is continuous.
Then $\{b\}=f^{-1}(W)$ is an open set --- a contradiction.

\parbf{\ref{ex:composition-continuous}.}
Apply the definitions.

\parbf{\ref{ex:Rge}}. Note that $S\in \s{T}$ if and only for any $x_0\le x_1$, if $x_0\in S$, then $x_1\in S$.

\parit{\ref{SHORT.ex:Rge:top}.}
Check the conditions in \ref{def:top-space} using the property above.

\parit{\ref{SHORT.ex:Rge:nonmetr}.}
Show that every two nonempty open sets in $(\RR,\s{T})$ intersect.
Show that the latter statement does not hold in a metric space with at least two points.
Make a conclusion.

\parit{\ref{SHORT.ex:Rge:mono}.}
To do the only-if part check the condition in \ref{def:cont-top}.

To do the if part, suppose $f$ is not nondecreasing;
that is, we can find $x_0<x_1$ such that $f(x_0)>f(x_1)$.
Note that the inverse image $V=f^{-1}([f(x_0),\infty))$ contains $x_0$ but does not contain $x_1$.
Conclude that $V$ is not open in $(\RR,\s{T})$, so $f\:(\RR,\s{T})\to(\RR,\s{T})$ is not continuous.

\parbf{\ref{ex:|x|}.}
Apply \ref{ex:composition-continuous} together with the fact that $x\mapsto |x|$ is a continuous real-to-real function (the latter is assumed to be known from calculus).

\parbf{\ref{ex:open-intersection+metrizable}}; \ref{SHORT.ex:open-intersection+metrizable:a}
Choose a metric that induces the topology on $\c{X}$.
Let $Q$ be a closed set in $\c{X}$.
Let $W_n$ be the union of $\tfrac1n$-balls centered at points in $Q$.
Show that $W_n$ is open and that $Q=\bigcap W_n$.

\parit{\ref{SHORT.ex:open-intersection+metrizable:b}.}
Try to find the needed closed set in the connected two-point space; see \ref{sec:ecamples(top)}.

\parbf{\ref{ex:closure-interior-complement}.}
Suppose $V$ is an open subset and $Q$ is its complement.
Recall that $Q$ is closed; see \ref{sec:closed-sets}.
Show and use that $V\subset A$ if and only if $Q\supset B$.

\parbf{\ref{ex:kuratowski}};
Show and use the following for any two subsets $A$ and $B$:
\begin{itemize}
\item $\mathring{\mathring A}=\mathring A\subset A\subset \bar A=\bar{\bar A}$.
\item if $A\subset B$, then $\bar A\subset \bar B$ and
$\mathring A\subset \mathring B$.
\end{itemize}

For the second part, try to choose a subset $A$ in $\RR$ so that it meets the following conditions:
\begin{itemize}
\item $A$ and $\RR\setminus A$ contain isolated points,
\item $A$ and $\RR\setminus A$ contain intervals,
\item $A$ and $\RR\setminus A$ are dense in some interval.
\end{itemize}


\parbf{\ref{ex:bry-closed}--\ref{ex:bry-bry}.}
Apply the definitions of boundary and closed set.

\parbf{\ref{ex:3=bry}.}
Read about the Cantor set (\ref{sec:Cantor set}) and think.

\parbf{\ref{ex:rel-int/cl}.}
Spell the definitions.
The needed examples can be found for $\c{A}=[0,1)\subset \RR=\c{X}$.

\parbf{\ref{ex:u-continuous}.}
Set $a=f|_A$ and $b=f|_{B}$.
Note that for any set $S\subset \c{Y}$, we have
\begin{align*}
a^{-1}(S)&=f^{-1}(S)\cap A,
\\
b^{-1}(S)&=f^{-1}(S)\cap B,
\\
f^{-1}(S)&=a^{-1}(S)\cup b^{-1}(S).
\end{align*}

\parit{\ref{SHORT.ex:u-continuous:open}.}
Choose an open set $W\subset \c{Y}$.
Show that $a^{-1}(W)$ and $b^{-1}(W)$ are open in $\c{X}$.
Conclude that $f^{-1}(W)$ is open.

\parit{\ref{SHORT.ex:u-continuous:closed}.}
Choose a closed set $Q\subset \c{Y}$.
Use \ref{prop:cont-closed} to show that $a^{-1}(Q)$ and $b^{-1}(Q)$ are closed.
Apply \mbox{\ref{def:closed-properties:u}} to show that $f^{-1}(Q)$ is closed.
Apply \ref{prop:cont-closed} again.

\parit{\ref{SHORT.ex:u-continuous:none}.}
Consider function $f$ that is constant on disjint sets $A$ and $B$ such that $\bar A\cap B\ne\emptyset$.

\parbf{\ref{ex:Cantor}.}
Use induction to show that $\c{C}_n$ is the set of reals $x\in [0,1]$ that can be written in base~$3$ without $1$ in the first $n$ digits.

For the second part, show that every $x\in \c{C}$ can be uniquely written in base~$3$ without using digit 1.
(Note that $(0.0222\ldots)_3=(0.1)_3$, so in general there is no uniqueness.)
Use the last statement to construct a bijection from $\c{C}$ to $\Power(\NN)$, which is the set of all subsets of the set of positive integers $\NN$.
By Cantor's theorem, $\Power(\NN)$ has greater cardinality than $\NN$.

\parbf{\ref{ex:bry-nbhd}.}
Let $A$ be a subset of a topological space $\c{X}$.
Denote by $B$ its complement; that is $B\z=\c{X}\setminus A$.
Show and use that the statement is equivalent to the following 
\[\c{X}\setminus\partial A =\mathring A\cup \mathring B.\]

\parbf{\ref{ex:dense-nbhd}.}
Apply the definitions of neighborhood and dense set.

\parbf{\ref{ex:lim-weaker}+\ref{ex:cocountable-top}.}
Apply the definition of the convergence.

\parbf{\ref{ex:bijection-ne-homeo}.}
Let $f\:\c{X}\to\c{Y}$ be a homeomorphism.
By the definition of inverse, we have $f^{-1}(f(x))=x$ and $f(f^{-1}(y))=y$ for any $x\in \c{X}$ and $y\in\c{Y}$.
Therefore, the existence of $f^{-1}\:\c{Y}\to\c{X}$ implies that $f$ is a bijection $\c{X}\leftrightarrow\c{Y}$.

For the second part,
try to find a continuous  bijection for the subspaces $A=[0,1)\cup \{2\}$ and $B=[0,1]$ of $\RR$.

\parbf{\ref{ex:exp}.}
Observe that $y\mapsto \ln y$ is the inverse of $x\mapsto e^x$,
and show that both functions are continuous.
(You can use that differentiable functions are continuous.)

\parbf{\ref{ex:arctan}.} 
Try to build the needed function from $x\mapsto \arctan x$ or $x\mapsto e^{-e^x}$

\parbf{\ref{ex:homeo=eq}.}
Apply the definitions of homeomorphism and \ref{ex:composition-continuous}.

\parbf{\ref{ex:square}+\ref{ex:inversion}.}
Learn about inversion transformation and try to apply it.

\parbf{\ref{ex:star-shaped}.}
Let $V$ be an open star-shaped set with respect to the origin.
Assume $V$ can be described by the inequality $r<f(\theta)$ in the polar $(r,\theta)$-coordinates, where $f\:\mathbb{S}^1\to \RR$ is a continuous function.
In this case it is not hard to prove the statement.
But a general star-shaped set, for example the one on the diagram is problematic.

\begin{Figure}
\vskip-0mm
\centering
\includegraphics{mppics/pic-71}
\end{Figure}

To do the general case,
show that $V$ can be presented as a union of a nested sequence of open sets $V_0\subset V_1\subset \dots$
such that each $V_n$ can be described by the inequality $r<f_n(\theta)$ in the polar $(r,\theta)$-coordinates with continuous $f_n\:\mathbb{S}^1\to \RR$.
We can assume that $V_0$ is a round disc around the origin.

Further construct a sequence of homeomorphisms $\phi_n\:V_{n-1}\to V_{n}$ such that
the compositions $\Phi_n=\phi_n\circ\dots\circ\phi_1\:V_0\to V_n$ stabilizes for each $x\in V_0$;
that is, $\Phi_n(x)$ is a fixed point for all sufficiently large $n$.
Set 
\[\Phi(x)=\lim_{n\to\infty} \Phi_n(x),\]
and show that $\Phi$ defines the needed homeomorphism $V_0\leftrightarrow V$.

\parit{Commnet.}
Note that the boundary of $V$ might be not homeomorphic the circle;
an example can be guessed from the picture.

\begin{Figure}
\vskip-0mm
\centering
\includegraphics{mppics/pic-72}
\end{Figure}

\parbf{\ref{ex:cont-dense}.}
Suppose that the sets are $P=\{p_1,p_2,\dots \}$ and $Q=\{q_1,q_2,\dots\}$.
Try to construct a sequence of homeomorphisms $\Phi_n\:\RR^2\z\leftrightarrow\RR^2$ such that
$\Phi_n$ converges to a homeomorphism $\Phi\:\RR^2\leftrightarrow\RR^2$ and for any $n$ we have $\Phi_n(\{p_1,\dots,p_n\})\z\subset Q$ and $\Phi_n^{-1}(\{q_1,\dots,q_n\})\z\subset P$.

\parbf{\ref{ex:bijective-closed-open}.}
Apply the definitions.

\parbf{\ref{ex:closed-open-cont}.}
Try the maps between two-point spaces with appropriate topologies.

\parbf{\ref{ex:closed-open-cont-R}.}
Construct a nonempty closed nowhere dense set $K$ without isolated points.
Map $K$ to $0$ and map each component of $\RR\setminus K$ by a homeomorphism to $\RR$.

\parit{Comment.}
If a function $f\:\RR\to \RR$ is closed and open, then it has to be continuous;
see the paper by Ivan Baggs \cite{baggs}.

\parbf{\ref{ex:cont-product-constr}.}
Apply the observation together with the facts that $(x,y)\mapsto x+y$, $(x,y)\mapsto x\cdot y$, and $(x,y)\mapsto \max\{x,y\}=\tfrac12\cdot |x+y|+\tfrac12\cdot |x-y|$ are continuous functions on $\RR^2$.

\parbf{\ref{ex:bryAxB}.}
Show and use that $\overline {A\times B}=\bar A\times \bar B$ and $(A\times B)^\circ=\mathring A\times \mathring B$.

\parbf{\ref{ex:cont-product}.} 
Check the following function
\[f(x,y)=
\begin{cases}
0&\text{if}\quad x=0\quad\text{or}\quad y=0,
\\
\tfrac xy &\text{if}\quad 0<x\le y,
\\
\tfrac yx &\text{if}\quad 0<y< x,
\end{cases}
\]

\parbf{\ref{ex:open-projection}.} Spell the definitions.

\parbf{\ref{ex:base-nbhd}}; \textit{if part.} 
Choose an open set $N$.
For any $x\in N$ choose an element $B_x$ of the base such that $x\in B_x\subset N$.
Observe that 
\[N=\bigcup_{x\in N}B_x.\]

\parit{Only-if part.} Suppose that $\s{B}$ is a base.
Then 
\[N=\bigcup_{\alpha}B_\alpha,\]
where $B_\alpha\in \s{B}$ for each $\alpha$.
Then for any $x\in N$ there is $\alpha$ such that $B_\alpha\ni x$;
in this case, $x\in B_\alpha\subset N$.

\parbf{\ref{ex:base-N}.}
Let $B_1=\{a_1,a_1+d_1,a_1+2\cdot d_1,\dots\}$ and $B_2=\{a_2,a_2+d_2,a_2+2\cdot d_2,\dots\}$.
Show that if $a\in B_1\cap B_2$, then
\[\{\,a, a+d, a+2\cdot d,\dots\,\}\subset B_1\cap B_2,\]
where $d=d_1\cdot d_2$.
Apply \ref{prop:base-any}.

Show that the set $\{1\}$ is closed but not open.

\parit{Comment.}
This topology was introduced by Harry Furstenberg \cite{furstenberg}; he used it to deduce from the last statement that the set of primes is infinite.
The resulting proof is not truly new; it is just the classical proof from Euclid's Elements written in the topological language.

\parbf{\ref{ex:graph}}; \ref{SHORT.ex:graph:prebase}
Observe that $(V\times \c{Y})\cap(\c{X}\times W) \z= V\times W$.
Further, show and use that all the sets $V\times W$ for open subset $V\subset \c{X}$ and $W\subset \c{Y}$ form a base in $\c{X}\times\c{Y}$.

\parit{\ref{SHORT.ex:graph:graph}.}
To show that the map $F$ is continuous,
apply \ref{ex:prebase} to the prebase described before the exercise.
Further, show and use that projection $G\:(x,f(x))\to x$ is a continuous left inverse;
that is, $G(F(x))=x$ for any $x$.

\parbf{\ref{ex:induced-nonmetrizable}.}
Show that \textit{every two disjoint closed sets of a metric space have disjoint open neighborhoods};
that is, for any two closed sets $A$ and $B$ there are open sets $V\supset A$ and $W\supset B$ such that $V\cap W=\emptyset$.
(Topological spaces that share this property are called \index{normal space}\emph{normal};
so you need to show that \textit{any metrizable space is normal}.)

Observe that arithmetic progression is a closed set in the initial topology.
Construct two disjoint arithmetic progressions that do not admit disjoint open neighborhoods. 

\parbf{\ref{ex:move-topology:pushforward}.} Check the conditions in \ref{def:top-space} directly.

\parbf{\ref{ex:pullback/pushforward}+\ref{ex:category-def-pull-push}.} Spell out the definitions.

\parbf{\ref{ex:open-closed-pushforward}.}
Since $f$ is continuous, $V=f^{-1}(W)$ is open for any open set $W\subset \c{Y}$.
It remains to show that if $V$ is open, then so is $W\subset \c{Y}$.

Note that $W=f(V)$.
If $f$ is open, then $W=f(V)$ is open as well.

Set $A=\c{X}\setminus V$ and $B=\c{Y}\setminus W$.
Since $V$ is open $A$ is closed.
Since $f$ is surjective, $B=f(A)$.
Since $f$ is a closed map, $B=f(A)$ is closed as well.
Therefore, $W=\c{Y}\setminus B$ is open.

\parbf{\ref{ex:eq-relation-f}.}
Check the conditions in the definitions of equivalence relation and equivalence class.

\parbf{\ref{ex:example/~}.}
Coose a subset $A\subset [0,1]$ with nonempty complement and let $x\sim y$ if $x,y\in A$ or $x,y\notin A$.
For part \textit{(a)}, choose $A$ to be open; for part \textit{(b)} chose a set that is neither open nor closed in $[0,1]$.

\parbf{\ref{ex:[0,1]/(0,1)}.}
Show that it has three points $a=[0]$, $b=[\tfrac12]$, and $c=[1]$ and the open sets are
\[\emptyset,\  \{b\},\ \{a,b\},\ \{b,c\},\ \text{and}\ \{a,b,c\}.\]

\parbf{\ref{ex:R/R}.}
Note that all positive numbers are in one $\RR_+$-orbit.
Similarly, all negative numbers are in one orbit and $0$ forms another orbit.
Thus, $\RR/\RR_+$ contains three points, say $p$, $n$, and $z$ that correspond to positive numbers, negative numbers, and zero.
It remains to describe all open subsets in $\{p,n,z\}$.

\parbf{\ref{ex:quotient-map}.}
Recall that
\[f(x)=[x]=G\cdot x=\set{g\cdot x}{g\in G}\]
Show that for any set $V\subset \c{X}$ we have
\[f^{-1}\circ f(V)=\bigcup_{g\in G}g\cdot V.\]

\parit{\ref{SHORT.ex:quotient-map:open}.}
Apply this formula to show that if $V$ is open, then so is $f^{-1}\circ f(V)$.
Finally apply the definition of quotient topology.

\parit{\ref{SHORT.ex:quotient-map:closed}.}
Apply this formula to show that if $G$ is finite and $V$ is closed, then so is $f^{-1}\circ f(V)$.

\parbf{\ref{ex:open-open-cover}.}
Apply \ref{def:top-space:u}, \ref{def:top-space:n} and \ref{def:cont-top}.

\parbf{\ref{ex:cofinite-compact}.}
Assume that the space is nonempty; otherwise there is nothing to prove.
Choose a nonempty set $V_0$ from the cover.
Its complement is a finite set, say $\{x_1,\dots,x_n\}$.
For each $x_i$ choose a set $V_i\ni x_i$ from the cover.
Observe that $\{V_0,V_1\dots,V_n\}$ is a subcover.



\parbf{\ref{ex:noncompact-un}}; \ref{SHORT.ex:noncompact-closed}.
Check subsets in a concrete space.

\parit{\ref{SHORT.ex:ucompact}.} Apply the definition of compact set.

\parit{\ref{SHORT.ex:noncompact-n}.}
Choose a noncompact space $\c{X}$ and consider topology on the union $\c{X}\cup\{a,b\}$ that includes
$\c{X}\cup\{a,b\}$, $\c{X}\cup\{a\}$, $\c{X}\cup\{b\}$ and all open sets in $\c{X}$.
Observe that the sets $\c{X}\cup\{a\}$ and $\c{X}\cup\{b\}$ are compact, but their intersection is not.

\parit{\ref{SHORT.ex:noncompact-metric}.} If $A$ is a subset of metric space $\c{M}$ that is not closed, then we can choose a point $p\in \bar A\setminus A$.
Check the definition of compactness for complements $V_n=\c{M}\setminus \cBall[p,\tfrac1n]$.

For the second part, try to use the argument in \ref{prop:compact-closed}.

\parbf{\ref{ex:nested-compact}.} Apply the finite intersection property.

\parbf{\ref{ex:proper-point}.}
Choose a family of closed sets in $\c{X}$, say $\{Q_\alpha\}$, with finite intersection property.
Assume $\bigcap Q_\alpha=\emptyset$.
Since $f^{-1}\{y\}$ is compact, for any $y\in \c{Y}$ we can choose a finite family $Q_{\alpha_1},\ldots, Q_{\alpha_n}$ such that $f^{-1}\{y\}\cap Q_{\alpha_1}\cap\z\ldots\cap Q_{\alpha_n}=\emptyset$.
Let $C_y=Q_{\alpha_1}\cap\ldots\cap Q_{\alpha_n}$.
Show that (1) $y\notin f(C_y)$ and (2) $f(C_y)$ is a closed set for every $y\in \c{Y}$.

Apply compactness of $\c{Y}$ to choose a finite set of points $y_1,\z\ldots,y_m\in \c{Y}$ such that $f(C_{y_1})\cap \z\ldots \cap f(C_{y_m})=\emptyset$.
Conclude that $C_{y_1}\cap \z\ldots \cap C_{y_m}=\emptyset$ and arrive at a contradiction.


\parbf{\ref{ex:disjoint-closed}.}
Let us write $A\Supset B$ if $\mathring{A}\supset \bar B$.

Construct a strongly nested sequence of intervals $[0,1]=[a_0,b_0]\Supset[a_1,b_1]\Supset\dots$ such that $[a_n,b_n]\cap Q_m=\emptyset$ for any $m\le n$.
Apply the finite intersection property (\ref{prop:finite intersection property}) to show that the intersection $X=[a_0,b_0]\cap [a_1,b_1]\cap\dots$ is nonempty.
Note that $X\cap Q_m=\emptyset$ for any $m$.

\parit{Comment.}
This is a close relative of the \index{Baire category theorem}\emph{Baire category theorem}.

\parbf{\ref{ex:fun(compact)}.} Apply \ref{prop:comp-image} and \ref{obs:closed-bounded}.

\parbf{\ref{ex:closed-bounded=compact}.}
Apply \ref{prop:compact-closed} and \ref{thm:comp-interval}.

\parbf{\ref{ex:one-point-comp}.}
Consider the map $h\:\RR\cup\{\infty\}\to \SS^1$ defined by $x\mapsto (\tfrac{2\cdot x}{1+x^2},\tfrac{1-x^2}{1+x^2})$ and $\infty\mapsto (0,-1)$.
Show that $h$ is a homeomorphism $\RR^\infty\to \SS^1$.

\parbf{\ref{ex:compact-product}.}
Apply \ref{prop:comp-image} to the projections $\c{X}\times\c{Y}\to\c{X}$ and $\c{X}\times\c{Y}\to\c{Y}$ (which are continuous).

\parbf{\ref{ex:inscribed-cover-base}.} Spell the definitions.

\parbf{\ref{ex:fake-proof(thm:compact-product)}.}
The last statement is wrong.
Show by example that in general, the product $V_{\beta_i}\times W_{\beta_i}$ do \textit{not} cover $\c{X}\times\c{Y}$.

\parbf{\ref{ex:S1-compact}};
\ref{SHORT.ex:S1-compact:comp-image}.
Write $\mathbb{S}^1$ is an image of closed interval under a continuous map, and apply \ref{prop:comp-image}.

\parit{\ref{SHORT.ex:S1-compact:compact-closed}.}
Use that $\mathbb{S}^1$ is a closed subset of $[-1,1]\z\times[-1,1]$.

\parbf{\ref{ex:closed-graph}.}
By \ref{prop:cont-closed}, it is sufficient to show that any closed set $A\subset\c{K}$ has closed inverse image $B=f^{-1}(A)\subset\c{X}$.

Observe that the set $C=\Gamma\cap (\c{X}\times A)$ is closed,
so its complement $U$ can be presented as a union $\bigcup V_\alpha\times W_\alpha$.

Suppose $B$ is not closed, choose a point $p\z\in \bar B\backslash B$.
Note that $\{p\}\times \c{K}$ is a compact set in~$U$.
Argue as in \ref{thm:compact-product} to prove that there is an open set $N_p\ni p$ such that $N_p\times \c{K}\subset U$.
Arrive at a contradiction.

\parit{Remark.}
This is a part of the \index{closed graph theorem}\emph{closed graph theorem}; compare to \ref{ex:hausdorff-diagonal} and \ref{ex:graph:graph}.
The following function $f\:\RR\to\RR$ has closed graph, but is not continuous:
\[f(x)=
\begin{cases}
\tfrac1x&\text{if}\ x\ne 0,
\\
0&\text{if}\ x= 0.
\end{cases}
\]
It shows that compactness of $\c{K}$ is a necessary assumption.

\parbf{\ref{ex:power-power}.}
If there is a converging subsequence $\mathfrak{p}_{i_1}$, $\mathfrak{p}_{i_2},\dots$,
then we can choose a set $S\subset \NN$ (so $S\in \c{A}$) such that $i_n\in S$ if $n$ is even and otherwise $i_n\notin S$.
Observe that $\mathfrak{p}_{i_n}\in \mathfrak{V}_{S}$ if $n$ is even and otherwise $\mathfrak{p}_{i_n}\in \mathfrak{W}_{S}=\c{B}\setminus \mathfrak{V}_{S}$.
Make a conclusion.

\parbf{\ref{ex:lebesgue=1}.}
Consider an infinite set of points with discrete metric.

\parbf{\ref{ex:product-sequentially-compact}.}
Given a sequence $z_n=(x_n,y_n)$, pass to a subsequence such that the $x$-coordinate converges.
Pass to a subsequence again to make the $y$-coordinate converge.
Show and use that the obtained sequence converges in $\c{X}\times\c{Y}$.

\parbf{\ref{ex:lindelof}}; \ref{SHORT.ex:lindelof:yes}.
Choose an open cover $\{V_\alpha\}$.
Let $W$ be the union of all intervals $(a,b)$ such that $(a,b)\subset V_\alpha$ for some $\alpha$ and $a,b\in\QQ$.
Show and use that $\c{X}\setminus W$ is countable.

\parit{\ref{SHORT.ex:lindelof:no}.}
Consider a cover of $\c{X}\times \c{X}$ by the following open sets:
\[[x,\infty)\times[-x,\infty)\ \text{and}\ (-\infty,x)\times(-\infty,-x)\]
for all $x\in\RR$.

\parbf{\ref{ex:non-contracting-map}.}
Consider two sequences of points $x_0,x_1,\dots$ and $y_0,y_1,\dots$ in $\c{M}$ such that $x_i\z=f(x_{i-1})$ and $y_i\z=f(y_{i-1})$ for any $i$.
Observe that
\[|x_0-y_0|\le |x_1-y_1|\le\ldots\]
Show that one can pass to subsequences $x_{i_1},x_{i_2},\dots$ and  $y_{i_1},y_{i_2},\dots$ such that $x_{i_n}\z\to x_0$ and $y_{i_n}\z\to y_0$ as $n\to\infty$.
Conclude that $|x_0-y_0|\z=|x_1-y_1|$; since the points $x_0$ and $y_0$ can be chosen arbitrary, it implies that $f$ is distance-preserving.

Observe that $f$ is injective.
Use the argument above to conclude that $f(\c{M})$ is dense in $\c{M}$.
Note that $f(\c{M})$ is compact and therefore closed (see \ref{prop:hausdorff-compact-closed}).
Conclude that $f$ is surjective and therefore bijective.

\parit{Comment.}
This statement is used to prove key properties of so-called \index{Gromov--Hausdorff metric}\emph{Gromov--Hausdorff metric} --- a metric on space of all (isometry classes of) compact metric spaces.

\parbf{\ref{ex:closed-complete}.}
We need to show that any Cauchy sequenc $q_1,q_2,\ldots$ in a closed subset $Q$ of a complete space $\c{M}$ converges to a point in $Q$.
Convergence follows from completness of $\c{M}$.
It remains to use that $Q$ is closed.

\parbf{\ref{ex:compact-complete}.}
If the space is not bounded, consider a sequence $x_1,x_2,\dots$ such that $|x_n-p|\to\infty$ for some (and therefore any) point $p$.

For the second part, check an infinite discrete space.

\parbf{\ref{ex:compact-net}.}
Choose a compact $\tfrac1{2\cdot n}$-net $K_n\subset \c{M}$.
By \ref{prop:seq-comp-metr} and \ref{lem:net}, $K_n$ contains a finite $\tfrac1{2\cdot n}$-net, say~$F_n$.
Observe that $F_n$ is a finite $\tfrac1n$-net of $\c{M}$;
that is,
\[\c{M}=\bigcup_{z\in F_n} \Ball(z,\tfrac1n).\]

Choose a sequence of points in $x_1,x_2,\dots$ in $\c{M}$.
Apply the \index{diagonal procedure}\emph{diagonal procedure} (should be known from calculus) to extract a subsequence $y_1,y_2,\dots$ such that for fixed choice $z_n\z\in F_n$ we have
$y_m\in \Ball(z_n,\tfrac1n)$ for any $m\ge n$.
Observe that $y_1,y_2,\dots$ is Cauchy and use completeness of $\c{M}$.

\parbf{\ref{ex:contracction}.}
Consider a sequence $x_0,x_1,\dots\in\c{M}$ such that $x_{i}=f(x_{i-1})$ for any $i$.
Show that this sequence is Cauchy.
Since $\c{M}$ is complete, the sequence has a limit, say $p$.
Observe that $p$ is a fixed point.
Furthermore, show that if $q\ne p$, then $|f(q)-p|<|q-p|$;
in particular, $f(q)\ne q$ and so $q$ is not fixed.

\parit{Comment.}
This is the \index{Banach fixed-point theorem}\emph{Banach fixed-point theorem}; it combines extreme simplicity and extreme usefulness.

\parbf{\ref{ex:alm-min}.}
Assume such point does not exist.
Then for any point $p$ there is a point $p'$ such that $|p-p'|<r(p)$ and $r(p')\le (1-\eps)\cdot r(p)$.
Let $p_0,p_1,\dots \in\c{M}$ be a sequence such that $p_i=p_{i-1}'$.
Show and use that this sequence is Cauchy.

\parit{Comment.}
This statement can be used on complete spaces in place of existence of minimum point of a continuous function on a compact space; as far as I know it was discovered by Ivar Ekeland \cite{ekeland}.



\parbf{\ref{ex:aa}}; \ref{SHORT.ex:aa:def}.
Apply \ref{thm:comp-interval} and \ref{ex:fun(compact):min-max}. Check the conditions in \ref{def:metric-space}.

\parit{\ref{SHORT.ex:aa:complete}.} Choose a Cauchy sequence $\gamma_1,\gamma_2,\dots\in\Gamma$.
Observe that $\gamma_n(t)$ is a Cauchy sequence in $\RR^2$ for any fixed $t\in [0,L]$.
Since $\RR^2$ is complete, we can define
\[\gamma_\infty(t)=\lim_{n\to\infty}\gamma_n(t).\]
Show that $\gamma_\infty\in\Gamma$.

\parit{\ref{SHORT.ex:aa:compact}.} Choose a sequence $\gamma_1,\gamma_2,\dots\in\Gamma$ and a countable dense set $\{t_1,t_2,\dots\}$ in $[0,L]$.
Apply the diagonal procedure to extract a subsequence $\gamma_{i_1}, \gamma_{i_2},\dots$ such that for any $t_t$ the sequence $\gamma_{i_n}(t_k)$ converges as $n\to\infty$.
Show that this subsequence converges in $\Gamma$.

\parbf{\ref{ex:round-disc}.}
Assume that the boundary curve $\gamma$ is not a circle.
Use convexity to show that all points of $\gamma$ except for a countable set $S$ are smooth; that is, their tangent lines is well-defined.
Show that there is a circle $\sigma$ that crosses $\gamma$ at three distinct smooth points; furthermore, at one of these points, the tangent lines of $\gamma$ and $\sigma$ are distinct.
Arrive at a contradiction.


\parbf{\ref{ex:hausdorff-unique-limit}.} Arguing by contradiction, assume a sequence has two limits $x$ and $y$.
Since the space is Hausdorff we can choose disjoint neighborhoods $V\ni x$ and $W\ni y$.
Since the sequence converges to $x$, the set $V$ contains all but finitely many elements of the sequence.
The same holds for $W$ --- a contradiction.

\parbf{\ref{ex:hausdorff-closure}.}
Let $V$ and $W$ be a pair of open sets and $W'=\c{X}\setminus \bar V$.
Show and use $V$ and $W$ meet \ref{def:hausdorff}
if and only if
$V$ and $W'$ meet \ref{def:hausdorff}.


\parbf{\ref{ex:hausdorff-diagonal}.}
The set $\Delta$ is closed if and only if its complement $U=(\c{X}\times\c{X})\setminus\Delta$ is open.
Show and use that the latter means that there is a family $\{(V_\alpha,W_\alpha)\}$ of disjoint pairs of open sets in $\c{X}$ such that
\[U=\bigcup_\alpha V_\alpha\times W_\alpha.\]

\parbf{\ref{ex:closed-graph-hausdorff}.}
Choose a point $(x,y_1)\notin \Gamma$.
Let $y_0=f(x)$.
Let $V\ni y_0$ and $W\ni y_1$ be as in the neighborhoods provided by Hausdorffness of $\c{Y}$.
Show that $f^{-1}(V)\times W$ is a neighborhood of $(x,y_1)$ that does not intersect $\Gamma$;
make a conclusion.

\parit{Remark.}
This is another part of the \index{closed graph theorem}\emph{closed graph theorem}; compare to \ref{ex:closed-graph} and \ref{ex:graph:graph}.

\parbf{\ref{ex:product} + \ref{ex:Hausdorff+inj}.} Spell the definitions.

\parbf{\ref{ex:normal-hausdorff}.}
By \ref{thm:hausdorff-compact-t3}, for any $y\in L$ there is a pair of open sets $V_y$, $W_y$ such that $V_y\supset K$ and $W_y\ni y$
such that $V_y\cap W_y=\emptyset$.
Mimic the proof of \ref{thm:hausdorff-compact-t3} using these pairs.

\parbf{\ref{ex:X/A}.} Apply \ref{obs:compact-to-hausdorff} for the following maps:

\parit{\ref{SHORT.ex:[0,1]/0=1}.}
The map $[0,1]\to \mathbb{S}^1$ defined by
\[t\mapsto (\cos (2\cdot\pi\cdot t), \sin(2\cdot\pi\cdot t)).\]

\parit{\ref{SHORT.ex:D/S}.}
The map $\DD^2\to \RR^3$ that is written from polar to spherical coordinates as
\[(r,\theta)\mapsto (1, \theta, \pi\cdot r).\]

\parit{\ref{SHORT.ex:SxI/S}.}
Map $(s,t)\in\SS^1\times [0,1]$ to the point in $\RR^2$ with the polar coordinates $(s,t)$.

\parbf{\ref{ex:convex-body}.}
Let $K\subset\RR^3$ be a convex body;
we can assume that the origin lies in the interior of $K$.

Show that the boundary $\partial K$ is compact.
Show that any half-line that starts from the origin intersects $\partial K$ at a single point.
Conclude that $x\mapsto \tfrac{x}{|x|}$ defines a continuous bijection $\partial K\to \SS^2$ and apply \ref{cor:cont-bijection=homeomorphism}.

\parbf{\ref{ex:1-point-comp-quotinet}.}
Use \ref{thm:hausdorff-compact-t3} to show that $\c{X}/Q$ is Hausdorff.
Denote the one-point compactification by $V^\infty$; it is a compact space.
Use \ref{cor:cont-bijection=homeomorphism} to show that the natural map $V^\infty\to \c{X}/Q$ is a homeomorphism.

\parbf{\ref{ex:compact-Hausdorff-closed}.}
Compactness of $\c{Y}$ follows from \ref{prop:comp-image}; it remains to show its Hausdorffness.

Choose two points $y_1,y_2\in\c{Y}$.
Observe that $K_1=f^{-1}\{y_1\}$ and $K_2=f^{-1}\{y_2\}$ are closed and therefore compact subsets of $\c{X}$.
Apply \ref{ex:normal-hausdorff} to get disjoint open sets $V_1\supset K_1$ and $V_2\supset K_2$.
Further, look at the sets $W_i=\c{Y}\setminus f(\c{X}\setminus V_i)$ and think.

\parbf{\ref{ex:first-countable-seq}.}
Let $N_1$, $N_2,\dots$ be the sequence of neighborhoods of $x$ provided by the definition of first-countable space.
If $x\in\bar A$, then we can choose $x_n\in N_n\cap A$ for every $n$.
Show that $x_n\to x$.
This proves the only-if part.
Show that the if part holds for any space, not necessary first-countable.

\parbf{\ref{ex:ezh}.}
Let $V_1,V_2,\dots$ be sequence of neighborhoods of $x_0=[0]\in \c{X}$.
Construct an open set $W$ in $\c{X}$ such that $V_n\not\subset W$ for any $n$.
Concule that $\c{X}$ is not first-countable.

\parbf{\ref{ex:distfun}.}
Argue as in \ref{ex:cont-product-constr} and do some algebra.
Take $V=f^{-1}(-\infty,\frac12)$ and $W=f^{-1}(\frac12,+\infty)$.

\parbf{\ref{ex:Т5}.}
Consider sets
\begin{align*}
V&=\bigcup_{a\in A} \Ball(a,\tfrac12\cdot \dist_Ba),
\\
W&=\bigcup_{b\in B} \Ball(b,\tfrac12\cdot \dist_Ab).
\end{align*}

\parbf{\ref{ex:haus-comp-bair}};
\ref{SHORT.ex:haus-comp-bair:Subset}. Apply \ref{thm:hausdorff-compact-t3}.

\parit{\ref{SHORT.ex:haus-comp-bair:bair}.}
Mimic the proof of \ref{thm:baire-category} using \ref{ex:nested-compact}.

\parbf{\ref{ex:irrational-baire}.} Enumerate rationals, say as $q_1,q_2,\dots$
Choose a sequence of open dense sets $V_1,V_2,\dots$ such that $V_i\not\ni q_i$ and apply the Baire theorem.

\parbf{\ref{ex:union-closures}.}
Apply the Baire theorem to the complements $V_n=\c{X}\setminus\bar B_n$.

\parit{Comment.}
This is the first step in the proof of the \index{uniform boundedness principle}\emph{uniform boundedness principle}.

\parbf{\ref{ex:Baire-subsets}};
\ref{SHORT.ex:Baire-subsets:open}.
Let $W$ be an open set in a Baire space $\c{X}$, and let $V_1,V_2,\dots$ be a sequence of open dense sets in $W$.
Apply the definition of Bair space to the sets $V_i'=V_i\cup (\c{X}\setminus W)^\circ$.

\parit{\ref{SHORT.ex:Baire-subsets:loc}.} Spell the definitons.

\parbf{\ref{ex:closed-G-delta}.} Argue as in \ref{ex:open-intersection+metrizable:a}.

\parbf{\ref{ex:G-delta=closed n dense-G-delta}}; \ref{SHORT.ex:G-delta=closed n dense-G-delta:closed}
Let $G=V_1\cap V_2\cap\dots$ be a G-delta set in a topological space $\c{X}$; here each $V_i$ is an open set.
Observe that $D=G\cup (\c{X}\setminus \bar G)$ is a dence G-delta set, $\bar G$ is closed, and
$G=\bar G\cap D$.

\parit{\ref{SHORT.ex:G-delta=closed n dense-G-delta:open}.}
Note that $S=[0,\infty)$ is a G-delta set in $\RR$.
Apply \ref{obs:G-delta} to show that $S$ is not an intersection of dence G-delta set with an open set in $\RR$.


\parbf{\ref{ex:uncountable-Gdelta}.}
Show and use that a complement of countable set in $\RR$ is G-delta.

\parbf{\ref{ex:continuity-Gdelta}};
\ref{SHORT.ex:continuity-Gdelta:cont}.
Denote by $V_n$ the union of all open sets $W\subset \RR$ such that $\diam f(W)<\tfrac1n$.
Observe that $V_n$ is open for any $n$.
Further, show and use that $\Cont f=V_1\cap V_2\cap\ldots$

\parit{\ref{SHORT.ex:continuity-Gdelta:R-Q}.} Apply \ref{ex:uncountable-Gdelta}.

\parit{\ref{SHORT.ex:continuity-Gdelta:Q}.} Show and use that for any integer $n$ and any irrational number $s$ there is $\eps>0$ such that $\eps$-neighborhood of $s$ contains none of the first $n$ rational numbers $q_1,\dots,q_n$.

\parbf{\ref{ex:Baire-closed-subsets}.}
Let $G$ be a G-delta subset of complete metric space.
Show and use that $G$ admits another metric that induces the same topology and makes $G$ to be a complete metric space.

For the second part, check the complement of $(\RR\setminus \QQ)\times \{0\}$ in $\RR^2$,
here $\QQ\subset\RR$ denotes the subset or rational numbers.

\parbf{\ref{ex:nonborel-image}.}
Let $\c{X}=\RR_d\times \RR$ where $\RR_d$ denotes the set $\RR$ equipped with the discrete topology.
Note that graph $\Gamma$ of any (not necessarily continuous) function $f\:\RR_d\to \RR$ is a closed set in $\c{X}$.
Show and use that one can choose $f$ so that the projection of $\Gamma$ to $\RR$ is any given set, say a Vitali set.

\parit{Comment.}
In fact there are Borel sets in $\RR\times \RR$, which projections to $\RR$ are not Borel, but this is out of scope of my notes.

\parbf{\ref{ex:borel-square}.}
Try to fix the following not very-well-defined map:
\[(0.d_1d_2\ldots)_2\to ((0.d_1d_3\ldots)_2,(0.d_2d_4\ldots)_2),\]
where $(0.d_1d_2\ldots)_2$ denotes a number in base 2.


\parbf{\ref{ex:connected->R}.}
Assume contrary.
Show and use that the sets $V=f^{-1}((0,\infty))$ and $W=f^{-1}((-\infty,0))$ form an open separating pair of $\c{X}$.

\parbf{\ref{ex:nested-in-compact}.}
Apply \ref{ex:nested-compact} to show that $Q_\infty$ is not empty.
Further, assume $V$ and $W$ separate $Q_\infty$.
Apply \ref{ex:nested-compact} to the sequence $Q'_n=Q_n\setminus(V\cup W)$.

\parbf{\ref{ex:colomb}.}
Let us denote by $\gcd(a,b)$ the greatest common divisor of positive integers $a$ and $b$.
Denote by $\NN_0$ the set of all nonnegative integers,
so the arithmetic progression  $\{a$, $a+d$, $a+2\cdot d,\dots\}$ can be written as $a+\NN_0\cdot d$.

\parit{\ref{SHORT.ex:colomb:base}.}
Suppose $\gcd(a_1,d_1)=\gcd(a_2,d_2)=1$.
Show that the set $(a_1+\NN_0\cdot d_1)\cap(a_2+\NN_0\cdot d_2)$ is either empty or it can be written as $a_3+\NN_0\cdot d_3$ for some positive integers $a_3$ and $d_3$ such that $\gcd(a_3,d_3)=1$.
Use it to check the conditions in \ref{prop:base-any}.

\parit{\ref{SHORT.ex:colomb:connected}.}
If a prime number $d$ is larger than $a_1$ and $a_2$, then $(a_1+\NN_0\cdot d)\cap (a_2+\NN_0\cdot d)=\emptyset$.
Use this to show that the space is Hausdorff.

Consider a finite sequence of elements of base $a_1+\NN_0\cdot d_1,\dots, a_n+\NN_0\cdot d_n$.
Show that the product $d_1\cdots d_n$ does not belong to any of them.
Apply \ref{ex:inscribed-cover-base} and \ref{obs:inscribed-cover} to conclude that the space is noncompact.

Finally, assume $V$ and $W$ separate $\NN$ with the described topology.
Observe that $a_1+\NN_0\cdot d_1\subset V$ and $a_2\z+\NN_0\cdot d_2\subset W$ for some positive integers $a_1$, $d_1$, $a_2$ and $d_2$ such that $\gcd(a_1,d_1)=\gcd(a_2,d_2)=1$.
Let $a_3=d_1\cdot d_2$;
without loss of generality we can assume that $a_3\in  V$.
Observe that $a_3+\NN_0\cdot  d_3\subset V$ for some $d_3$ such that $\gcd(a_3,d_3)=1$.
Show that $(a_3+\NN_0\cdot d_3)\cap (a_2+\NN_0\cdot d_2)\ne \emptyset$ and arrive at a contradiction.

\parit{Comment.}
This problem and solution with more details are discussed in the note by Solomon Colomb \cite{golomb}

\parbf{\ref{ex:quotient-connected}.}
Apply \ref{prop:image-connected}.

\parbf{\ref{ex:connected-product}.}
Given a separating pair of $\c{X}$ or $\c{Y}$ construct a separating pair of $\c{X}\times\c{Y}$; this will prove the if part.
For the only-if part, apply \ref{prop:union-connect} to show that $(\{x\}\times \c{Y})\cup(\c{X}\times \{y\})$ is connected for any $x\in\c{X}$ and $y\in \c{Y}$.
And apply it again to show that
$\c{X}\times \c{Y}$ is connected.

\parbf{\ref{ex:R-connected}+\ref{ex:connected-star-shaped}.}
Apply \ref{prop:connected[0,1]}, \ref{prop:image-connected}, and \ref{prop:union-connect}.

\parbf{\ref{ex:nonopen-connected-component}};
\ref{SHORT.ex:nonopen-connected-component:closed}
Apply \ref{ex:A<B<bar-A}.

\parit{\ref{SHORT.ex:nonopen-connected-component:open}.}
Consider the following subspace of real line $A=\{0,1,\tfrac12,\tfrac13,\dots\}$.
Show that the one-point set $\{0\}$ is a connected component in $A$ and it is not open in $A$.

\parit{\ref{SHORT.ex:nonopen-connected-component:finite}.}
Apply  \ref{SHORT.ex:nonopen-connected-component:closed}.

\parit{Comment.}
The Cantor set $\c{C}$ (see \ref{sec:Cantor set}) gives a more interesting example in \ref{SHORT.ex:nonopen-connected-component:finite}.
Each connected component of $\c{C}$ is a one-point set, but $\c{C}$ does not have isolated points.

\parbf{\ref{ex:quasi-component}.}
Denote by $Q_x$ the intersection of all clopen sets containing $x$.
Observe that $Q_x$ is closed.
Assume $Q_x$ is not connected; subdivide it into two disjoint closed sets;
apply \ref{ex:normal-hausdorff} and try to reach a contradiction.

The closure of the following set in $\RR^2$ gives a noncompact Hausdorff example:
\[\set{(x,n+\tfrac1{1-x^2})}{n\in \ZZ\ \text{and}\  x\in (-1,1)},\]
here $\ZZ$ denotes the set of integers.
For a compact, but not Hausdorff example, consider the set $\c{X}=\ZZ\cup\{+\infty,-\infty\}$;
declare any subset of $\ZZ$ and any complement of a finite set to be open.

\parit{Comment.}
The set $Q_x$ is called the \index{quasi-component}\emph{quasi-component} of $x$;
note that $y\in Q_x$ if and only if $f(x)=f(y)$ for any continuous map $f$ from $\c{X}$ to the discrete two-point space.

\parbf{\ref{ex:R2neR}.}
Show that $\RR^2$ has no cut points, but $\RR$ has.

\parbf{\ref{ex:not-homeo}.}
Count points with given number of components in the complement for each space.

\parbf{\ref{ex:Q/H}.}
Let $x$ be the cross point,
let $c\ne x$ be a point on the circle,
let $e$ be one of two endpoints of the segment,
and let $i$ be a point on the segment that is distinct from $x$ and not an endpoint.
Show that $Q/H$ has just 4 points $[x]$, $[c]$, $[e]$, and $[i]$ (argue as in \ref{ex:not-homeo}).
List the open subsets of $\{[x], [c], [e], [i]\}$.

\parbf{\ref{ex:sierpinski}};
\ref{SHORT.ex:sierpinski:connected}
Let $T_n$ be the union of all sides of the $3^n$ triangles after $n$-th iteration.
Note that the sequence is nested; that is, $T_0\subset T_1\subset \dots{}$
Use induction to show that each $T_n$ is connected.
Conclude that the union $T=T_0\cup T_1\cup\dots$ is connected.
Finally, show that Sierpi\'nski triangle is the closure of $T$ and apply \ref{ex:A<B<bar-A}.

\parit{\ref{SHORT.ex:sierpinski:homeogroup}.}
Denote the Sierpi\'nski triangle by~$\triangle$.

\begin{Figure}
\vskip-0mm
\centering
\includegraphics{mppics/pic-51}
\end{Figure}

Let $\{x,y,z\}$ be a 3-point set in $\triangle$ such that $\triangle \setminus\{x,y,z\}$ has 3 connected components.
Show and use that there is a unique choice for the set $\{x,y,z\}$ and 
it is formed by the midpoints of the original triangle.

\parbf{\ref{ex:simple-path}.}
Passing to a subinterval, we can assume that $a(t)\ne x$ and $a(t)\ne y$ if $0<t<1$.
Show that there is a maximal (with respect to inclusion) open set $V\subset [0,1]$ such that if $(t_0,t_1)$ is a connected component of $V$, then $a(t_0)=a(t_1)$.
Consider map $\hat a\:[0,1]\to \c{X}$ defined by
$\hat a(t)=a(t)$ if $t\notin V$ and $\hat a(t)=a(t_0)$ if $t$ lies in a connected component $(t_0,t_1)$ of $V$.
Show that $\hat a$ is a path.
Read about Devil's staircase and apply similar construction to make $\hat a$ injective,
or follow the argument in the note by Reinhard Börger \cite{borger}; see also \cite{brazas}.

\parit{Comment.}
From this statement we get that any path-connected Hausdorff space with at least two points must have at least continuum of points.
Note that connected two-point space provides an example of finite path-connected space.
On the other hand, \ref{ex:colomb} provides an example of countable connected space.


\parbf{\ref{ex:two-point-path-connected}.}
Recall that connected two-point space has two point $a$ and $b$,
and its open sets are $\emptyset$, $\{a\}$, and $\{a,b\}$.
Show and use that $f\:[0,1]\z\to\c{X}$ defined by
\[f(t)=
\begin{cases}
a&\text{if\ }t<1,
\\
b&\text{if\ }t=1
\end{cases}
\]
is a continuous map.

\parbf{\ref{ex:image(path-connected)}.}
Show and use that for any continuous map $\phi$ and any path $a$, the composition $\phi\circ a$ is a path.

\parbf{\ref{ex:product(path-connected)}.}
Suppose that $a$ and $b$ are paths in $\c{X}$ and $\c{Y}$ respectively.
Show and use that $t\z\mapsto (a(t),b(t))$ is a path in $\c{X}\times\c{Y}$.

\parbf{\ref{ex:flea-and-comb}.} The flea and the comb.

\parbf{\ref{ex:nonpathconnected-closure}.} Look at the picture and think


\begin{Figure}
\vskip-0mm
\centering
\includegraphics{mppics/pic-1030}
\end{Figure}

\parbf{\ref{ex:path-connected-component-closed-open}.}
Choose path-connected component $V$.
Assume its complement, say $W$, is nonempty.
Observe that $W$ is a union of path-connected components.
Therefore, $W$ is open, and the pair $V$, $W$ separates the space.

To prove the converse, apply \ref{thm:conncted/path-connected}

\parbf{\ref{ex:monks}.}
Read the note by Tatsuo Homma \cite{homma} and think.

\parit{Comments.}
This is a well known puzzle; with the following interpretation, it appears in the book of
Peter Winkler \cite[p. 121]{winkler}:
\textit{Two monks climb the mountain on the same day, starting at the same time and altitude, but on different paths.
The paths go up and down on the way to the summit (but never dip below the starting altitude).
Can they vary their speed (sometimes going backwards) so that at every moment of the day they are at the same altitude?}

\parbf{\ref{ex:QQ}.}
Show and use that for any rational numbers $r\ne 0$ and $q$, the line $y=r\cdot x+q$ lies in $A\cup B$.

\parbf{\ref{ex:pseudocircle}.} Read about \index{pseudo-arc}\emph{pseudo-arc} and think.

\parbf{\ref{ex:finite-connecte}.}
Describe paths with two-point images and use this description to construct a path between given two points in a connected finite space.

\parbf{\ref{ex:locally-path-connected}}; \ref{SHORT.ex:locally-path-connected:comb}
Show that the local path-connectedness holds at all points of comb, but does not hold at the flea.

\parit{\ref{SHORT.ex:locally-path-connected:not}.} Consider closure of the comb in $\RR^2$.

\parit{Comment.}
Try to construct a path-connected space that is not locally path-connected at any point; can you make it compact?

\parbf{\ref{ex:open-path-connected-component}.}
Apply \ref{obs:open-connected-component} and \ref{thm:open-connected=path-connected}.

\parbf{\ref{ex:homot-sphere}.}
Consider linear homotopy in $\RR^3$:
\[f_t(x)=(1-t)\cdot f_0(x)+t\cdot f_1(x).\]
Observe that $f_t(x)\ne 0$ for any $t$ and $x$.
Compose $f_t$ with the radial projection $\RR^3\setminus\{0\}\to\SS^2$ defined by $y\mapsto \tfrac y{|y|}$.


\parbf{\ref{ex:homot-S1}.}
Argue as in \ref{ex:homot-sphere} to construct a homotopy from $f$ to the antipodal map $x\mapsto -x$.
Further, use rotation of $\SS^1$ to construct a homotopy from the antipodal map to the identity map.
Consider concatenation of these two homotopies.

\parbf{\ref{ex:retract-closed}.}
Choose $x\in \c{X}$; let $y=r(x)$.
Assume $y\ne x$.
Let $V\ni x$ and $W\ni y$ be disjoint neighborhoods provided by Hausdorffness.
Show and use that the set $V\cap r^{-1}(W)$ contains $x$ and does not intersect $r(\c{X})$.

\parbf{\ref{ex:retract-strong-weak}};
\ref{SHORT.ex:retract-strong-weak:retract}.
Consider homotopy $h_t$ defined by
\[h_t(x)=
\begin{cases}
x&\text{if\ }x\in J_0
\\
t\cdot x&\text{if\ }x\notin J_0
\end{cases}
\]

\parit{\ref{SHORT.ex:retract-strong-weak:weak}.}
Assume $J_\infty$ is deformation retract;
let $h_t$ be the corresponding homotopy relative to $J_\infty$.
Show that for any large $n$ we have
\[|h_t(1,\tfrac1{2^n})-(1,0)|_{\RR^2}<\tfrac1{10}\] for any $t$
and arrive at a contradiction.

\parit{\ref{SHORT.ex:retract-strong-weak:triangle}.}
Assume that $F$ is a retract of the square.
Show that in this case $J_\infty$ is a deformation retract of $F$;
the latter contradicts \ref{SHORT.ex:retract-strong-weak:weak}.


\parbf{\ref{ex:contractible}}.
Let $h_t\:\c{X}\to \c{X}$ be a homotopy from the identity map to a constant map,
so, $h_1(x)=p$ for any $x\in \c{X}$

\parit{\ref{SHORT.ex:contractible:path-connected}.}
Show and use that $t\mapsto h_t(x)$ is a path from $x$ to $p$.

\parit{\ref{SHORT.ex:contractible:target}.}
Choose two continuous maps $f_0, f_1\:\c{Y}\z\to \c{X}$.
Note that $h_t\circ f_i$ is a homotopy from $f_i$ to the constant map $y\mapsto p$.
Concatenate the obtained homotopies.

\parit{\ref{SHORT.ex:contractible:sourse}.}
Choose two continuous maps $f_0, f_1\:\c{X}\z\to \c{Y}$.
Note that $f_i\circ h_t$ is a homotopy from $f_i$ to the constant map $x\mapsto y_i=f_i(p)$.
Choose a path from $y_1$ to $y_2$ and use it to construct a homotopy from $f_0\circ h_1$ to $f_1\circ h_1$.
Concatenate the obtained homotopies.

\parbf{\ref{ex:homotopy-type}.}
Spell the definitions.

\parbf{\ref{ex:homotopy-type-contractible}.}
Spell the definitions.

\parbf{\ref{ex:homotopy-equivalence}.} Show and use that $h\sim h\circ f\circ g\sim g$.

\parbf{\ref{ex:8-theta}.}
Observe and use that both sets form retracts of the plane figure in the picture.

\begin{Figure}
\vskip-0mm
\centering
\includegraphics{mppics/pic-1260}
\end{Figure}

\parbf{\ref{ex:mapping-cylinder}.}
Read about mapping cylinder.

\parbf{\ref{ex:ab=e}.}
Apply the claim.


\parbf{\ref{ex:assoc+neutral}}; \ref{SHORT.ex:assoc}.
Let $d=(a*b)*c= a*(b*c)$.
Show that
\begin{align*}
d([0,\tfrac{t_0}2])&\supset d([0,t_0])\ \text{and}
\\
d([0,1-t_0])&\supset d([0,1-\tfrac{t_0}2])
\end{align*}
if $0<t_0\le \tfrac12$.
Apply Hausdorffness.

\parit{\ref{SHORT.ex:neutral}.}
Use induction to show that given integer $n\ge 1$ we have $a(t)=p$ for any $t\le 1-\tfrac1{2^n}$.
Apply Hausdorffness.

\parit{Comment.}
It is instructive to check that both statements fail in the connected two-point space.

\parbf{\ref{ex:group}.}
Consider the map $f\:[0,1]\times [0,1]\z\to \c{G}$ defined by
$f\:(s,t)\mapsto a(t)\cdot b(s)$.
Show that $f$ is continuous.
Write both $a*b$ and $b*a$ as a composition of $f$ with a path from $(0,0)$ to $(1,1)$ in $[0,1]\times [0,1]$ and apply linear homotopy.

\parbf{\ref{ex:contractible=>sc}.} Spell both definitions.

\parbf{\ref{ex:S1=>sc}.} The only-if part follows directly from \ref{prop:sc=1hclass}.
It remains to prove the if part.

Let us identify $\SS^1$ with $[0,1]/\{0,1\}$ (see \mbox{\ref{ex:[0,1]/0=1}});
so any a loop is also a map $\SS^1\to \c{X}$; let us use the same notation for both.

Let $h_t\:\SS^1\to \c{X}$ be a (free) homotopy from $a\:\SS^1\to \c{X}$ to a constant map $e_p$.
Consider the path $b(t)\df h_t(0)$.
Show and use that
$a\sim b*e_p*\bar b\sim e_{x_0}$.

\parbf{\ref{ex:van-kamp}.}
Recall that if the product of paths is not parenthesized, then it is taken in the usual order;
that is, \index{$a*b*c*d$} $a*b*c*d\df((a*b)*c)*d$.

\begin{Figure}
\vskip-0mm
\centering
\includegraphics{mppics/pic-1350}
\end{Figure}

\parit{\ref{SHORT.ex:van-kamp:loops}.}
Show that any loop can be subdivided into small arcs $a_1,\dots,a_n$ such that each $a_i$ lies completely in $V$ or $W$ and has ends in $V\cap W$ (use here \ref{prop:lebesgue-number} if needed).
We may assume that each $a_i$ is parametrized by $[0,1]$, thus our loop is homotopic to $a_1*\ldots*a_n$.
Connect $x_0$ to every point $x_i=a_i(1)$ for $i<n$ by a path $b_i$ in $V\cap W$.
Apply the claims in \ref{sec:homotopy-claim} to show that
\[a_1*\ldots*a_n{\sim}(a_1*\bar b_1)*(b_1*a_2*\bar b_2)*\ldots*(b_{n-1}*a_n).\]
Observe that each loop $a_1*\bar b_1$, $b_1*a_2*\bar b_2, \z\dots, b_{n-1}*a_n$ lies entirely in $V$ or $W$.

\parit{\ref{SHORT.ex:van-kamp:sc}.}
Let $a_i$ and $b_i$ be as above.
Since $V$ and $W$ are simply-connected, $a_1*\bar b_1\sim e_{x_0}$, $b_1*a_2*\bar b_2\sim e_{x_0}, \z\dots, b_{n-1}*a_n\sim e_{x_0}$.
Hence $a_1*\ldots*a_n\sim e_{x_0}$.

\parit{\ref{SHORT.ex:van-kamp:s2}.}
Subdivide $\SS^2$ into subsets $V$ and $W$ defined by $z>-\tfrac12$ and $z<\tfrac12$, respectively.
Observe that $V$ and $W$ are homeomorphic to an open disc and the intersection $V\cap W$ is homeomorphic to the product $\SS^1\times (0,1)$ and apply \ref{SHORT.ex:van-kamp:sc}.

\parit{\ref{SHORT.ex:van-kamp:s2d}.}
Choose a simply-connected open sets $V\supset \SS^2$ and  $W\supset \DD^2$ and argue as in \ref{SHORT.ex:van-kamp:s2}.

\parit{Comment.}
Part \ref{SHORT.ex:van-kamp:sc} is a special case of Van Kampen's theorem; it calculates the fundamental group of $\c{X}$ in terms of fundamental groups of $V$, $W$, $V\cap W$ and the homomorphisms
$\pi_1(V\cap W,x_0)\to\pi_1(V,x_0)$, $\pi_1(V\cap W,x_0)\to\pi_1(W,x_0)$ induced by the inclusions $V\cap W\hookrightarrow V$ and $V\cap W\hookrightarrow W$.
See also \ref{ex:vankam-nonconnect}.

\parbf{\ref{ex:comp-induced}.} Spell the definitions.

\parbf{\ref{ex:retract*}}; \ref{SHORT.ex:retract*:0}. Show and use that $r\circ a=a$ for any loop $a$ in $A$.

\parit{\ref{SHORT.ex:retract*:i}.} Let $a_\tau$ be a homotopy in $\c{X}$ between two loops $a_0$ and $a_1$ in $A$.
Show and use that $r\circ a_\tau$ is a homotopy in $A$ between the same loops.

\parit{\ref{SHORT.ex:retract*:ri}.}
By \ref{SHORT.ex:retract*:0} and \ref{SHORT.ex:retract*:i}, it is sufficient to show that $r_*$ is a monomorphism and $i_*$ is an epimorphism.

The first statement means that if $r\circ a_0\sim r\circ a_1$ in $A$ for some loops $a_0$ and $a_1$ in $\c{X}$, then $a_0\sim a_1$ in $\c{X}$.
Let $r_\tau$ be the homotopy from $r_0=\id_{\c{X}}$ to $r_1=r$.
Show and use that $r_\tau\circ a_i$ is a homotopy from
$a_i$ to $r\circ a_i$.

The same homotopies $r_\tau\circ a_i$ can be used to show that $i_*$ is an epimorphism.

\parbf{\ref{ex:uphi}}; \ref{SHORT.ex:uphi:uphi} Show and use that
$f_1\circ a\sim \bar c*(f_0\circ a)*c$ for any loop $a$ in $\c{X}$ based at $x_0$.

\parit{\ref{SHORT.ex:uphi:hom-type}.}
Apply \ref{SHORT.ex:uphi:uphi} for homotopy equivalence $\c{X}\z\to\c{Y}$ and its homotopy inverse.

\parbf{\ref{ex:hom-product}.} Spell the definitions.

\parbf{\ref{ex:sim-product}.}
Let $f\:\c{X}\times\c{X}\to \c{Y}$ be the quotient map.
Choose a base point $f(x,x)\in \c{Y}$.
Given two loops $a$ and $b$ based at $x$ in $\c{X}$ denote by $(a,b)$ the loop in $\c{X}\times \c{X}$ defined by $t\mapsto (a(t),b(t))$.
Observe that $f\circ (a,e_x)= f\circ (e_x,a)$ for any loop $a$.
Use it to show that
\[f\circ(a*b,e_x)\sim f\circ(a,b)\sim f\circ(b*a,e_x)\]
and make a conclusion.

\parbf{\ref{ex:S1}}; \ref{SHORT.ex:S1:R3>R^2}.
Suppose there is a homeomorphism $h\:\RR^3\to\RR^2$.
Choose a point $p\in \RR^3$ and let $q=h(p)$.
Observe that $\RR^3\setminus\{p\}$ is homeomorphic to $\RR^2\setminus\{q\}$.
Show that $\pi_1(\RR^3\setminus\{p\})$ is trivial, but $\pi_1(\RR^2\setminus\{q\})$ is not and arrive at a contradiction.

\parit{\ref{SHORT.ex:bry->bry}.}
Apply linear homotopy, \ref{ex:contractible=>sc}, and \ref{thm:pi1(S1)}.

\parit{\ref{SHORT.ex:S1:finite-complement}.}
Apply linear homotopy and \ref{thm:pi1(S1)}.

\parbf{\ref{ex:KK}}; \ref{SHORT.ex:K!=K}.
Assume there is a homeomorphism $h\:K_1\to K_2$.
Argue as in \ref{ex:not-homeo} and \ref{ex:bry->bry} to show that
(1) $h$ maps interior points of $K_1$ to interior points of $K_2$,
(2) $h$ maps boundary points of $K_1$ to boundary points of $K_2$,
and (3) $h$ maps the attached segments of $K_1$ to attached segments of $K_2$.
Try to arrive at a contradiction.

\parit{\ref{SHORT.ex:K=K}.}
Realize that both products $K_1\times [0,1]$ and $K_2\times [0,1]$ are solid tori with attached pairs of squares.
To construct a homeomorphism, twist one solid torus.

\parbf{\ref{ex:missed-point}.}
Apply \ref{lem:covering}.
For the second part, let $b$ go around $\SS^1$ and come back the same way.

\parbf{\ref{ex:lift(S1)}.}
Assume $f$ exists.
Consider a loop $a$ in $\SS^1$ based at $1$ with nonzero degree.
Then $\tilde a\df f\circ a$ is its lift.
Applying a shift to $f$ we can assume that $\tilde a(0)=0$,
but then $\deg a=\tilde a(1)=f\circ a(1)=f\circ a(0)=0$ --- a contradiction.

\parbf{\ref{ex:wind}}; \ref{SHORT.ex:wind:constant}.
Apply \ref{thm:open-connected=path-connected} and \ref{thm:pi1S1Z}.

\parit{\ref{SHORT.ex:wind:7}.} Observe that the winding number vanish in the unbounded component,
and when one crosses the curve the winding number increase or decrease by $1$, depending on the direction of the curve at the point of crossing.

\parit{Comment.}
Observe that if the winding around the point is not zero, then it lies in the image of any continuous map $\DD^2\to\CC$, assuming that the boundary $\SS^1=\partial\DD^2$ maps along the given curve.

\parbf{\ref{ex:covering-clopen}.}
Apply \ref{thm:covering-open} to show that the image and its complement are open.

\parbf{\ref{ex:n-folded}.}
Apply the definition of the coverign map to show that the set of points in $\c{X}$ with exactly $n$ points in their inverse image has to be open and close.

\parbf{\ref{ex:covering-product}.}
Spell the definitions of covering map and product topology.

\parbf{\ref{ex:induced-hom-covering}};
\ref{SHORT.ex:induced-hom-covering:injective}+\ref{SHORT.ex:induced-hom-covering:lift}+\ref{SHORT.ex:induced-hom-covering:action}.
Reformulate each statement using homotopy of paths and apply \ref{lem:path-lifting}.

\parbf{\ref{ex:2-folded}.}
Let $w\:\tilde{\c{X}}\to \c{X}$ be a 2-fold cover.
Consider the involution $i$ that swaps the points in each inverse image $w^{-1}\{x\}$ for $x\in \c{X}$.
Prove that $i$ generated a $\ZZ_2$-action on $\tilde{\c{X}}$ and identify $\c{X}$ with the quotient space $\tilde{\c{X}}/\ZZ_2$.

\parbf{\ref{ex:2-folded-s1}.}
Apply \ref{obs:compact-to-hausdorff} and \ref{cor:covering-finite-action}.

\parbf{\ref{ex:3-fold(8)}.}
The solutions are not unique.
Observe that both types of arrows must go into each crossing point, and both types of arrows must go out of each crossing point.
Any labeling that meets this condition will do the job.
Two of coverings are normal with group $\ZZ_3$, and one is not.

\parbf{\ref{ex:4-fold(8)}.}
The solutions are not unique.
Choose spaces from the following picture (or draw your own).
Label arcs by arrows, depending on the choice of labeling the same space might solve different parts of the exercise.

\begin{Figure}
\vskip-0mm
\centering
\includegraphics{mppics/pic-1665}
\end{Figure}

\parbf{\ref{ex:Borsuk-Ulam}}; \ref{SHORT.ex:Borsuk-Ulam:Borsuk-Ulam}
Assume the contrary and apply the Borsuk--Ulam theorem for the map
\[\tilde h(x)=\frac{f(x)- f(-x)}{|f(x)- f(-x)|}.\]

\parit{\ref{SHORT.ex:ABC}.} Show and use that if $f(x)=f(-x)$, then $x$ and $-x$ lie in one of the sets $A$, $B$, or $C$.
For the last part, try to cover sphere by four spherical triangles.

\parit{Comments.}
Part \ref{SHORT.ex:Borsuk-Ulam:Borsuk-Ulam} of the exercise can be interpreted the following way:
\textit{at any moment of time there exists a pair of antipodal points on the surface of the earth that have the same temperature and pressure.}

\parbf{\ref{ex:reasonable}.}
Apply \ref{obs:covering:connected}.

\parbf{\ref{ex:Hawaiian/bouquet}.}
Show and that $\c{F}_\infty$ is reasonable and apply \ref{clm:earring}.

\parbf{\ref{ex:Hawaiian-nouni}.}
A sufficiently small circle $S\subset \c{H}$ must lie in an open set evenly covered by $w$.
Construct a retraction $r\:\c{H}\to S$ consider a nontrivial connected covering $v$ with base $S$ and pass to its pullback along $r$ (apply \ref{ex:pull-back}).

\parbf{\ref{ex:Hawaiian-comp-cover}.}
Taking pullback of the worping map along a retraction $r\:\c{H}\to S$ tosome circle $S\subset \c{H}$ produces a normal $\ZZ$-covering.
The total space will look like a cactus train.
Now the for the double cover, unwarp different sircles in cacti of this train, so that the sice of circle approach zero when you move to infinity.

\parbf{\ref{ex:griffiths}.}
Read about the Griffiths space.

\parbf{\ref{ex:reasonable-diagram}.}
Apply \ref{obs:covering:connected} and once \ref{clm:sc-base}.

\parit{Comment.}
Each statemnt fails if $\c{Z}$ is not reasonable.
Exercise \ref{ex:Hawaiian-comp-cover} provides an example for part \ref{SHORT.ex:reasonable:u}.
The other parts are simpler, but require patience.
Recall that $\NN^\infty$ denoted one-point compactification of positive integers $\NN$;
this space can be identified with $\{0,1,\tfrac12,\tfrac13,\dots\}\subset\RR$;
this space is not locally connected and therefore not reasonable.

For \ref{SHORT.ex:reasonable:v}, take $\c{X}=\NN^\infty\times \NN$, $\c{Y}=\NN^\infty\times \{0,1\}$, and $\c{Z}=\NN^\infty$;
furtheremore, define $u$ and $w$ as projections and let
\[v(n,k)
=
\begin{cases}
(n,0)&\text{if}\ n\le k
\\
(n,1)&\text{if}\ n> k
\end{cases}
\]
Observe that $(\infty,0)\in \c{Y}$ does not admit a neighborhood evenly covered by $v$.

For \ref{SHORT.ex:reasonable:w}, take $\c{X}=\NN^\infty\times \NN$, $\c{Y}=\c{X}/\sim$ where $(k,m)\sim(k,n)$ if $m>k$ and $n>k$, and $\c{Z}=\NN^\infty$;
furtheremore, let $u$ be the projection and let $v$ be the quotient map.
Observe that $\infty\in \c{Z}$ does not admit a neighborhood evenly covered by $w$.


\parbf{\ref{ex:vankam-nonconnect}.}
Suppose $V\cap W=A\cup B$, where $A$ and $B$ are disjoint open sets.
Take 2 copies of each set: $A_1, A_2,B_1,B_2,V_1,V_2,W_1,W_2$.
The inclusions of $A$ and $B$ in $V$ and $W$ induce 8 embdeddings:
$A_i\hookrightarrow V_j$, $A_i\hookrightarrow W_j$, $B_i\hookrightarrow V_j$, $B_i\hookrightarrow W_j$
for all pairs $i$ and $j$.
Glue the copies along all the embdeddings on the diagram and show that the obtained space overs $\c{X}$ twice.
\[
\begin{tikzcd}
A_1\arrow[d, hook'] \arrow[r, hook] & V_1 & B_1\arrow[l, hook'] \arrow[d, hook]\\
W_1&&W_2\\
B_2\arrow[u, hook] \arrow[r, hook] & V_2 & A_2\arrow[l, hook'] \arrow[u, hook']
\end{tikzcd}
\]

\parit{Comment.}
This is another partial case of Van Kampen theorem, see also \ref{ex:van-kamp:s2d}.

One can also construct a normal $\ZZ$-covering, preparing $\ZZ$ copies of each set and glue them along the following maps:
\begin{align*}
\dots\hookleftarrow A_{n-1} &\hookrightarrow V_{n-1}\hookleftarrow B_{n-1}\hookrightarrow
\\
 \hookrightarrow W_n \hookleftarrow A_n &\hookrightarrow V_n\hookleftarrow B_n\hookrightarrow
\\
\hookrightarrow W_{n+1}\hookleftarrow A_{n+1}&\hookrightarrow V_{n+1}\hookleftarrow \dots
\end{align*}

\parbf{\ref{ex:group-unicover}.}
Let $a\cdot b(t)\df a(t)\cdot b(t)$ and consider the product $[a]\cdot [b]\df[a\cdot b]$ on $\tilde{\c{G}}$.

\parbf{\ref{ex:given-monodromy}.}
The fundamental group $\pi_1(\c{Z},z_0)$ acts on the total space $\tilde{\c{Z}}$ of the universal covering.
Equip $G$ with the descrete topology.
The product $\tilde{\c{Z}}\times G$ admits a natural action of $\pi_1(\c{Z},z_0)\times G$;
this action is freely discontinuous.
Pass to the quotient of $\tilde{\c{Z}}\times G$ by the image of homomorphism $\pi_1(\c{Z},z_0)\to\pi_1(\c{Z},z_0)\times G$ defined by $\gamma\mapsto (\gamma,m(\gamma))$.

\parbf{\ref{ex:tietze}.}
Follow the proof and see that compactness of $\Gamma\cap \cBall[x,R]$ is the only place we use special properties of the Euclidean plane.
This was used to prove the existence of points $q_n$ and $s_n$.
Use instead their approximate versions $\tilde q_n$ and $\tilde s_n$, so the equalities in \ref{eq:abF} hold with an error that approaches zero as $n\to\infty$, and check that the remaining part of the proof works with these points.

\parbf{\ref{ex:nonclosed-Gamma}.}
One needs open complements to apply \ref{ex:van-kamp:s2d} and \ref{ex:vankam-nonconnect}.

If $\Gamma$ is not closed, then its complement might be connected, but not path connected.
For example, the complement of $\Gamma$ can be $W'$ --- the flea and comb discussed in \ref{sec:flea and comb}.
In this case the complement $\RR^3\setminus\Gamma$ formed by two open half-spaces joint by $W'$;
it is easy to see that a loop that cross the $(x,y)$ at flea and returns thru comb is not contractible in $\RR^3\setminus\Gamma$.

If the complement $\RR^2\setminus\Gamma$ is the union of two copies of fan $F$ (see \ref{ex:retract-strong-weak}) as shown on the picture
then it is path-connecteed, but $\RR^3\setminus\Gamma$ is not simply conneccted.
Try to bootstrap a  noncntractibel loop in it.

\begin{Figure}
\vskip-0mm
\centering
\includegraphics{mppics/pic-1920}
\end{Figure}

\parbf{\ref{ex:3-components-Gamma}.}
Show and use that $Y$ can be retracted to figure eight; see \ref{sec:Figure eight}.

\parbf{\ref{ex:klee[0,1]-1}.} Follow the proof of \ref{prop:home-gamma-z-axis}.

\parbf{\ref{ex:connected-complement}.}
Assume the contrary.
Identify $\RR^2$ with the $(x,y)$-plane in $\RR^3$.
Show the following:
\begin{itemize}
 \item $\RR^3\setminus(A\cup B)$ has nontrivial fundamental group, but
 \item $\RR^3\setminus A$ and $\RR^3\setminus B$ have trivial fundamental groups.
\end{itemize}

Construct a continuous function $f\:\RR^2\z\to[0,1]$ such that $A=f^{-1}\{0\}$ and $B=f^{-1}\{1\}$.
Consider the homeomorphism $h\:\RR^3\selfmap$ defined by $(x,y,z)\mapsto (x,y,z+f(x,y))$.
Observe that $h$ moves $A$ and $B$ to different horizontal planes.
Use that triviality of fundamental groups of $\RR^3\setminus A$ and $\RR^3\setminus B$ to show that
$\RR^3\setminus h(A\cup B)$ has trivial fundamental group.
Arrive at a contradiction.

\parit{Comment.}
A more standard solution is done by applying Alexander duality to Čech cohomology.

\parbf{\ref{ex:jordanI+}.}
Apply \ref{ex:klee[0,1]-1}.

\parit{Comment.}
In fact any compact contractible set in $\RR^2$ has connected complement;
a way more general statement is provided by Alexander's duality theorem.

\parbf{\ref{ex:proper-jordan3}.} Apply \ref{ex:3-components-Gamma}.

\parbf{\ref{ex:klee[0,1]}}; \ref{SHORT.ex:klee[0,1]:[0,1]} Apply \ref{ex:jordanI+}.

\parit{\ref{SHORT.ex:klee[0,1]:bry}.}
Assume the contrary.
Then there is a neighborhood $V$ of a point $p\in J$ that does not meet all components of $\SS^2\setminus J$.
Choose a subset $I\subset J$ that is homeomorphic to $[0,1]$ and such that $I\supset J\setminus V$ and apply \ref{SHORT.ex:klee[0,1]:[0,1]}.

\parit{Comment.} Recall that by \ref{ex:proper-jordan3}, $\SS^2\setminus J$ has exactly two connected components, but the solution above does not use this.

%\parbf{\ref.}

%\parbf{\ref.}

%\parbf{\ref.}

%\parbf{\ref.}

\spell{\end{multicols}}{}
