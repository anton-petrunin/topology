
\begin{subthm}{ex:induced-hom-covering:sc}
Assume  $\tilde{\c{X}}$ is simply connected.
Let $b$ be a loop in $\c{X}$ based at $x_0$ and $\tilde b$  be its lift in $\tilde{\c{X}}$ such that $\tilde b(0)=\tilde x_0$.
Show that $[b]=1\in\pi_1(\c{X},x_0)$ if and only if $\tilde b$ is a loop.
\end{subthm}



\begin{subthm}{ex:induced-hom-covering:endpoint}
Let $a_0$ and $a_1$ be two paths in $\c{X}$ such that $a_0(0)=a_1(0)=x_0$ and $a_0(1)=a_1(1)$.
Suppose $\tilde a_0$ and $\tilde a_1$ be lifts of $a_0$ and $a_1$ such that $\tilde a_0(0)=\tilde a_1(0)=\tilde x_0$.
Show that $\tilde a_0(1)=\tilde a_1(1)$ if and only if $[a_0*\bar a_1]\in w_*(\pi_1(\tilde{\c{X}})$.
\end{subthm}











Each of the following claims proved by explicit construction of the needed homotopy.
Each time the homotopy constructed as a composition $h\circ s_\tau(t)$, where $h$ is a fixed path and  $s_\tau$ is a one-parameter family of functions $[0,1]\to [0,1]$.
The graphs of $s_\tau$ provide more intuitive descriptions of the families;
the formulas presented just to make it formally correct.

Recall that $\eps_p$ is the constant path with image $p$;
that is, $\eps_p(t)=p$ for any $t$.


\begin{wrapfigure}{r}{30mm}
\centering
\vskip-5mm
\includegraphics{mppics/pic-30}
\end{wrapfigure}

\parit{Proof.}
Consider the function
\[s_\tau(t)=
\begin{cases}
2\cdot \tau \cdot t&\text{if}\ t\le \tfrac12,
\\
2\cdot\tau-1+2\cdot (1-\tau) \cdot t&\text{if}\ t\ge \tfrac12.
\end{cases}
\]
Observe that $(\tau,t)\mapsto s_\tau(t)$ and therefore $(\tau,t)\mapsto f(s_\tau(t))$ are continuous maps.
Therefore $h_\tau(t)=f(s_\tau(t))$ is a homotopy.

Further,
\begin{align*}
f(s_0(t))&=\eps_p*f(t),
\\
f(s_{\frac12}(t))&=f(t),
\\
f(s_1(t))&=f(t)*\eps_q
\end{align*}
for any $t$.
Whence the claim follows.
\qeds




\begin{wrapfigure}[4]{r}{30mm}
\centering
\vskip-10mm
\includegraphics{mppics/pic-31}
\end{wrapfigure}

\parit{Proof.}
Consider the function
\[s_\tau(t)=
\begin{cases}
2\cdot \tau \cdot t&\text{if}\ t\le \tfrac12,
\\
1-2\cdot \tau \cdot t&\text{if}\ t\ge \tfrac12.
\end{cases}
\]
Observe that $(\tau,t)\mapsto s_\tau(t)$ and therefore $(\tau,t)\mapsto f(s_\tau(t))$ are continuous maps.

Note that $f(s_1(t))=f*\bar f(t)$ for any $t$.
Therefore $h_\tau(t)=f(s_\tau(t))$ is a homotopy from $\eps_p$ to $f*\bar f$.

It proves the first statement.
The second statement follows from the first one since $\bar{\bar f}=f$.
\qeds

The product of paths is not associative;
that is, in general,
\[(f*g)*h\ne f*(g*h)\]
for paths $f,g,h$ such that both products are defined.
In other words, we have to specify the order of product.
The following claim says that \textit{product of paths is not associative up to homotopy}.


\begin{wrapfigure}[3]{r}{30mm}
\centering
\vskip-4mm
\includegraphics{mppics/pic-32}
\end{wrapfigure}

\parit{Proof.}
Consider the function $s_\tau$ defined by
\[s_\tau(t)=
\begin{cases}
\tfrac{1+\tau}2\cdot t&\text{if}\ t\le \tfrac12,
\\
\tfrac{\tau-1}4+t&\text{if}\ \tfrac 34\ge t\ge \tfrac12,
\\
\tau-1+ (2-\tau)\cdot t&\text{if}\  t\ge \tfrac34.
\end{cases}
\]
Observe that $(\tau,t)\mapsto s_\tau(t)$ and therefore $(\tau,t)\mapsto f*(g*h)(s_\tau(t))$ are continuous maps.

Note that $s_1(t)=t$,
and therefore \[f*(g*h)(t)\z=f*(g*h)(s_1(t))\] for any $t$.
Further,
\[(f*g)*h(t)\z=f*(g*h)(s_0(t))\] for any $t$.
It remains to observe that $f*(g*h)(s_\tau(t))$ is the needed homotopy.
\qeds
























\begin{thm}{Exercise}\label{ex:distfun}
Consider two subsets $A,B\subset \c{M}$ such that $\bar A\cap B=A\cap \bar B=\emptyset$.

\begin{subthm}{ex:distfun:T5}
Assume $A,B\subset \c{M}$ be two subsets such that $\bar A\cap B=A\cap \bar B=\emptyset$.
Consider the sets
\begin{align*}
V=\bigcup_{a\in A} \Ball(a,\tfrac12\cdot \dist_Ba),
&
W=\bigcup_{b\in B} \Ball(b,\tfrac12\cdot \dist_Ab).
\end{align*}
Show that $V$ and $W$ are open $V\supset A$, $W\supset B$, and $V\cap W$
\end{subthm}

\end{thm}



\begin{thm}{Advanced exercise}\label{ex:path-connected-component-closed-open:closed}
Consider the \index{lexicographical order}\emph{lexicographical order} $\prec$ on $\RR^2$:
\[
(x_1,y_1)\prec(x_2,y_2)
\quad\Leftrightarrow\quad
x_1<x_2\quad \text{or}\quad x_1=x_2\ \text{and}\  y_1<y_2.
\]
Show that the following sets for all pairs $(x_1,y_1)\prec(x_2,y_2)$ in $\RR^2$
\[I_{(x_1,y_1),(x_2,y_2)}\df\set{(x,y)\in \RR^2}{(x_1,y_1)\prec(x,y)\prec(x_2,y_2)}\]
form a base of some topology $\s{T}$ on $\RR^2$.

Furthermore, show that the squre $([0,1]\times[0,1]$ with the topology induced by $\s{T}$ is compact, Hausdorff, connected, but not path-connected, and each path-conencted component in $([0,1]\times[0,1],\s{T})$ is a veritcal interval $\{x\}\times[0,1]$.
\end{thm}


\parbf{\ref{ex:path-connected-component-closed-open:closed}.}
Check the conditions in \ref{prop:base-any}.
To prove compactness, try to modify the proof of \ref{thm:comp-interval}.
Hausdorfness follows from the definition.
To prove connectedness, modify the proof of \ref{prop:connected[0,1]}.
To show that the space is not path-connected, use that any path contains at most countable subsets of isolated points.
Finally observe that every path-connected component in $[0,1]\times[0,1]$ is a vertical segment $\{x\}\times [0,1]$.











\begin{thm}{Exercise}\label{ex:open-close}
Let $f\:[0,1)\to [0,1)$ be an arbitrary function (not necessary continuous) such that $f(x)>x$ for every $x$.
Show that there is a unique subdivision of $[0,1)$ into disjoint semiopen intervals of the type $[x,f(x))$.
\end{thm}

\parbf{\ref{ex:open-close}.}
Let $I\ni 0$ be the maximal subinterval of $[0,1)$ such that $I$ admits a unique cover by intervals $[x_\alpha,f(x_\alpha))$ with $x_\alpha\in I$ for all $\alpha$.

Show that $I$ is open and closed.
Conclude that $I=[0,1)$.

\parit{Comment.}
This is an application of the so-called \index{open-closed argument}\emph{open-closed argument}; it provides a geometric version of induction (or transfinite induction).










\section{An application}

According to the legend, Queen Dido was allowed to claim land bounded by oxhide,
and she fenced the land for Carthage on the sea shore with a long strip made from oxhide.

\begin{wrapfigure}{r}{23 mm}
\vskip-5mm
\centering
\includegraphics{mppics/pic-770}
\end{wrapfigure}

\begin{thm}{Dido's Problem}\label{thm:dido}
Show that among all plane curves of given length $L$, a semicircle {}\emph{enclose} the greatest area;
we say that figure $F$ is enclosed by a curve $\gamma$ if it is bounded by $\gamma$ and the line segment between its endpoints.

\end{thm}

\begin{thm}{Claim}\label{clm:dido1}
Suppose a curve $\gamma$ encloses the largest area among all curves of length $\le L$.
Then $\gamma$ is a semicircle of length $L$.
\end{thm}

The proof uses elementary geometry.

\parit{Proof.}
Let $F$ be enclosed by $\gamma$.
Let us show that if the area is largest, then $F$ is convex.
Indeed, assume $F$ is not convex.
Then one can enlarge its area by shortening $\gamma$ (as shown in the picture).

\begin{wrapfigure}{r}{26 mm}
\vskip-0mm
\centering
\includegraphics{mppics/pic-775}

\bigskip

\includegraphics{mppics/pic-776}

\bigskip

\includegraphics{mppics/pic-777}
\end{wrapfigure}

Denote by $a$ and $b$ the endpoints of $\gamma$.
Let $x$ be another point on~$\gamma$.

Let us show that $\measuredangle axb=\tfrac\pi2$.
Cut from $F$ two parts $F_a$ and $F_b$ with chords $ax$ and $bx$, so $F$ is subdivided into $F_a$, $F_b$ and $\triangle axb$.
In particular, $\area F=\area F_a+\area F_b+\area \triangle axb$.
Think that $F_a$ and $F_b$ are attached to the sides of $\triangle axb$, we have a joint in $x$ and can rotate one side of the triangle.
If the angle of $\triangle axb$ at $x$ is not right, then a slight rotation makes area of the triangle bigger;
therefore, we can enlarge enclosed area of the curve.

Let $\alpha$ and $\beta$ be the angles of $\triangle axb$ at $a$ and $b$, respectively.
Since $\measuredangle axb=\tfrac\pi2$, we have $\alpha+\beta=\tfrac\pi2$.
Draw a line from $x$ to a point $m$ on the opposite side of the triangle such that $\measuredangle axm=\alpha$ and therefore $\measuredangle bxm=\beta$.
Triangle $\triangle amx$ and $\triangle bmx$ are isosceles since each has a pair of equal angles.
Therefore, $|a-m|=|x-m|=|b-m|$.
In particular $m$ is the midpoint of $a$ and $b$, and $x$ lies on a semicircle with diameter $ab$.
It follows that $\gamma$ is a semicircle.

It remains to observe that among semicircle of length $\le L$, the semicircle of maximal length maximizes the enclosed area.
\qeds

\begin{thm}{Claim}\label{clm:dido2}
Among all plane curves of length $\le L$ there is a curve that maximizes the enclosed area.
\end{thm}

\parit{Proof.}
Any plane curve $\gamma$ with length at most $L$ can be equipped with a parametrization $\gamma\:[0,L]\to\RR^2$ such that
\[|\gamma(t_0)-\gamma(t_1)|_{\RR^2}\le |t_0-t_1|.\]
Consider the space $\Gamma$ of all such parametrized curves with the metric defined by
\[|\gamma_1-\gamma_2|_\Gamma= \max\set{|\gamma_1(t)-\gamma_2(t)|_{\RR^2}}{t\in[0,L]}\]

\begin{thm}{Exercise}
Prove the following statements:
\begin{subthm}{}
The formula defines a metric on $\Gamma$; in particular, the maximum in the right-hand side of the formula is defined.
\end{subthm}

\begin{subthm}{}
The space $\Gamma$ is complete.
\end{subthm}

\begin{subthm}{}
Any closed and bounded set in $\Gamma$ is compact.%
\footnote{A metric space $\c{M}$ is called \index{proper space}\emph{proper} if any bounded closed set in $\c{M}$ is compact;
so, this part states that \textit{$\Gamma$ is proper}.}
\end{subthm}

\end{thm}

Each point in $\Gamma$ is a curve.
Choose a point $a$ and consider the set $K_a$ of all curves $\gamma$ in $\Gamma$ such that $\gamma(0)=a$.
Observe that $K_a$ is a closed and bounded set in $\Gamma$;
applying the exercise we get that $K_a$ is compact.

It remains to prove the following claim and apply \ref{ex:fun(compact):min-max}.
\qeds

We will need a more precise definition of enclosed area, that will work for curves with self-intersections.
Namely, given $\gamma\in \Gamma$, denote by $F_\gamma$ the maximal closed set with boundary contained in the union of $\gamma([0,L])$ and the line segment from $\gamma(0)$ to $\gamma(L)$.

\begin{thm}{Claim}\label{clm:dido3}
$\gamma\mapsto \area F_\gamma$ defines a continuous function $\Gamma\to\RR$.
\end{thm}


\parit{Proof.}
It is sufficient to show that
\[|\gamma_1-\gamma_2|_\Gamma<L/n
\quad\Rightarrow\quad
\area F_{\gamma_2}\le \area F_{\gamma_1}+8\cdot\pi\cdot L^2/n
\eqlbl{eq:area-cont}\]
for any $\gamma_1,\gamma_2\in \Gamma$.

\begin{wrapfigure}{r}{46 mm}
\vskip-5mm
\centering
\includegraphics{mppics/pic-790}
\end{wrapfigure}

Note that $F_{\gamma_2}$ lies in an $\tfrac Ln$-neighborhood of $F_{\gamma_1}$;
moreover if $p\in F_{\gamma_2}\setminus F_{\gamma_1}$, then $|p-x|<\tfrac Ln$ for some $x\in\partial F_{\gamma_1}$.
Since the length of $\gamma$ is at most $L$, so it the line segment from $\gamma(0)$ to $\gamma(L)$.
Whence we can choose an $2\cdot n$-point $L/n$-net of $\partial F_{\gamma_1}$, say $\{x_1,\dots,x_{2\cdot n}\}$.
It follows that
\[F_{\gamma_2}\setminus F_{\gamma_1}\subset \bigcup_{i=1}^{2\cdot n}\Ball(x_i,2\cdot L/n).\]
Since $\area\Ball(x,r)_{\RR^2}=\pi\cdot r^2$ we get
\[\area (F_{\gamma_2}\setminus F_{\gamma_1})\le 2\cdot n\cdot\pi \cdot (2\cdot L/n)^2,\]
and \ref{eq:area-cont} follows.
\qeds
















\parbf{\ref{ex:prebase};} \textit{only-if part.}
Apply that a prebase is a collection of open sets.

\parit{If part.} Show that for any finite collection of sets $P_1,\dots,P_n$ in the prebase the inverse image
$f^{-1}(P_1\cap\dots\cap P_n)$
is open.
Further apply \ref{ex:base-nbhd}.















\begin{thm}{Exercise}\label{ex:nowhere-dense}
Classify topological spaces (up to homeomorphism) containing a unique nowhere
dense subset.
\end{thm}

\parbf{\ref{ex:nowhere-dense}.}
In other words, we need to classify spaces such that any nonempty subset is everywhere dense.
Observe that every point of the space lies in every nonempty open set.
Conclude that the space has concrete topology.
Show that any such space has this property.







\section{Homotopy of paths}




so, homotopy of paths a continuous map defined on the unit square $[0,1]\times [0,1]$.


The definition of relative homotopy will be applied mainly to the unit segment with subset formed by its ends;
that is, $\c{X}=[0,1]$ and $A=\{0,1\}$.

Let $\c{Y}$ be a topological space with chosen point $y_0$.
A path $f\:[0,1]\z\to \c{Y}$ will be called \index{loop}\emph{loop} with base at $y_0$ if $f(0)=f(1)=y_0$.
The set of homotopy classes relative to $\{0,1\}$ is called the \index{fundamental group}\emph{fundamental group} of the space $\c{Y}$ and is denoted by
$\pi_1(\c{Y},y_0)$.
Soon we will equip this set with multiplication and show that it is indeed a group.
As a rule, the fundamental group is considered only for path-connected $\c{Y}$.

The product of two loops $f$ and $g$ is defined as follows as their conctination $f*g$ (see ???): first we travel along $f$, and then along $g$.

Associativity of the product holds up to homotopy; that is, altho in general $f*(g*h)\ne (f*g)*h$, we still have that $f*(g*h)\sim (f*g)*h$

The identity element is the class of the constant map $[0,1]\to \c{Y}$ with value $y_0$.
The inverse element is represented by the loop traversed in the opposite direction.
In the followng sections all this will be discussed more formally.













\section{Definitions}

Assume $f_t\:\c{X}\to \c{Y}$ be a one-parameter family of maps, $t\in [0,1]$.
If the map $[0,1]\times \c{X}\to \c{Y}$, defined as $(t,x)\mapsto f_t(x)$ is continuous, then $f_t$ is called a \index{homotopy}\emph{homotopy} of maps from $\c{X}$ to $\c{Y}$.

If $f_t(a)$ is independent of $t$ for any point $a$ in some subset $A\subset \c{X}$, then we say that  $f_t$ is a {}\emph{homotopy relative to $A$}.

Two maps $g,h\:\c{X}\to \c{Y}$ are called {}\emph{homotopic} (briefly $g\sim h$)
if there is a homotopy $f_t\:\c{X}\to \c{Y}$ such that $g=f_0$ and $h=f_1$.

Two maps $g,h\:\c{X}\to \c{Y}$ are called \emph{homotopic relative to} $A$ (briefly, $g\sim h\rel A$)
if there is a homotopy $f_t\:\c{X}\to \c{Y}$ relative to $A$ such that $g=f_0$ and $h=f_1$.

\begin{thm}{Exercise}\label{ex:hom-eq}
Show that ``$\sim$''  defines equivalence relations on the set of continuous maps between given topological spaces.
That is, for any continuous maps $f,g,h\:\c{X}\to \c{Y}$, we have
\begin{itemize}
\item $f\sim f$,
\item if $f\sim h$, then $h\sim f$, and
\item if $f\sim g$ and $g\sim h$, then $f\sim h$.
\end{itemize}
Do the same for ``$\sim\rel A$''.
\end{thm}

Suppose that $f_\tau\:[0,1]\to\c{X}$ is homotopy of paths relative to its ends;
that is, $f_\tau(0)$ and $f_\tau(1)$ do not depend on $t$.
Then we say that $f_0$ is \index{homotopy of paths}\emph{homotopic} to $f_1$, briefly $f_0\sim f_1$.
This is shortcut notation --- formally speaking, we had to say \textit{homotopic rel. $\{0,1\}$} and write $f_0\sim f_1\rel{\{0,1\}}$,
















\section{Finite spaces}\label{sec:finite}

Finite spaces provide a source of examples of topological spaces.
They also have some distinguishing properties, which we about to discuss.

Let $\c{F}$ be a finite topological space;
that is, a finite set equipped with a topology.

Note that $\c{F}$ contains finitely many distinct open sets.
Since intersection of finite collection of open sets is open,
we can conclude that arbitrary intersection of open sets in $\c{F}$ is open.

Similarly, arbitrary union of closed sets in $\c{F}$ is closed.

In other words, for the finite topological spaces $\c{F}$, open and closed sets have the same properties.
In particular one could define a new topology on $\c{F}$ by declaring all open sets to be closed and the other way around.

Given a point $x\in \c{F}$,
denote by $\bar x$ the closure of one-point set $\{x\}$.

For two points $x,y\in \c{F}$ write $x\succcurlyeq y$ if $\bar x\supset \bar y$.
The introduced relation ``$\succcurlyeq$'' behaves very much as ``$\ge$'' --- formally speaking it defines a \index{preorder}\emph{preorder} on $\c{F}$;
that is, for any $x,y,z\in\c{F}$ the following conditions are fulfilled:
\begin{itemize}
\item $x\succcurlyeq x$ (reflexivity).
\item if $x\succcurlyeq y$ and $y\succcurlyeq z$, then $x\succcurlyeq z$ (transitivity).
\end{itemize}

Conversely, if $\c{F}$ is a finite set with preorder ``$\succcurlyeq$'',
one can define topology on $\c{F}$ by declaring that for any $\bar x=\set{y\in \c{F}}{x\succcurlyeq y}$ and all its finite intersections and unions are closed.
Thus the partial order is an equivalent way to describe topology.



























\section{Quotients by subsets}

Intuitively, quotient space is the space obtained by gluing equivalent points together.
For example, consider the \emph{minimal equivalence relation} on $[0,1]$ such that $0\sim 1$;
that is, $x\sim y$ if and only if one of the following conditions hold $x=y$, or $x=0$ and $y=1$, or $x=1$ and $y=0$.
Then the quotient space $[0,1]/{\sim}$ is homeomorphic to 
\[\mathbb{S}^1=\set{(x,y)\in\RR^2}{x^2+y^2=1}.\]
A homeomorphism is induced by the map $[0,1]\to \mathbb{S}^1$
\[f(t)=\left(\cos(2\cdot\pi\cdot t),\sin(2\cdot\pi\cdot t)\right).\]
The latter statement can be proved directly from the definition of quotient topology, but soon we will prove the following statement that implies this and similar statements effortlessly.
So we suggest to wait with proof of this statement.

\begin{itemize}
\item Suppose $\c{X}$ is a \emph{compact space} and  $\c{Y}$ is \emph{Hausdorff space} and $f\: \c{X}\to\c{Y}$ is a surjective continuous map.
Then $\c{Y}$ is equipped with the quotient topology.
\end{itemize}















In other words, $f\:\c{X}\to \c{Y}$ is an \emph{emebedding} if it is injective and the topology on $\c{X}$ coinsides with pullback topology for $f$.

A function $f\:\c{X}\to \c{Y}$ is called local embedding if for any point $x\in \c{X}$ there is a neighborhood $U\ni x$ such that the restriction $f|_U$ is an embedding.\footnote{The meaning of the word \emph{local} in topology can be described the following way: a space $\c{X}$ is \emph{locally magnificent} if for any point $x\in \c{X}$ and any neighborhood $V\ni x$ there is a smaller neighborhood $W$ (that is, $x\in W\subset V$) which is \emph{magnificent}.}
 
 
 
 
 
 
 
 
 
 
 
 
The following proposition provides a way to construct the weakest topology containing the given collection of sets.

\begin{thm}{Proposition}
Let $\s{Q}$ be arbitrary collection of subsets of a set $\c{X}$.
Consider the set $\s{P}$ of all finite intersections of sets in $\s{Q}$;
that is, $P\in \s{P}$ if there is a finite collection of subsets $Q_1,\dots,Q_n\in \s{Q}$ such that 
\[P=Q_1\cap\dots\cap Q_n.\]
If the collection is empty we assume that $P=\c{X}$.

Denote by $\s{O}$ all possible unions of sets in $\s{P}$;
that is, $O\in \s{O}$ if there is, a (possibly infinite) collection of sets $P_\alpha\in\s{P}$, $\alpha\in\c{I}$ such that 
\[O=\bigcup_{\alpha\in\c{I}}P_\alpha.\]
Then $\s{O}$ is a topology on $\c{X}$.
\end{thm}

\parit{Proof.}
We need to check if the collection $\s{O}$ satisfies the conditions in the definition \ref{def:top-space}.

The conditions \ref{def:top-space:empty} and \ref{def:top-space:u} hold evidently.
It remains to check condition \ref{def:top-space:n};
that is, given two sets $O,O'\in\s{O}$ we need to show that $O_1\cap O_2\in \s{O}$.

Let
\[O=\bigcup_{\alpha\in \c{I}}V_\alpha,\quad O'=\bigcup_{\beta\in \c{J}}V'_\beta,\]
where  $V_\alpha,V'_\beta\in\s{P}$ for any $\alpha\in\c{I}$ and $\beta\in\c{J}$.

Fix $\alpha\in\c{I}$ and $\beta\in\c{J}$.
Since $V_\alpha,\in\s{P}$ it can be presented as an intesection
\[V_\alpha=Q_1\cap \dots \cap Q_n,\]
with $Q_1,\dots,Q_n\in \s{Q}$.
Similarly, 
\[V'_\beta=Q'_1\cap \dots \cap Q'_m,\]
with $Q'_1,\dots,Q'_m\in \s{Q}$.
It follow that 
\begin{align*}
W_{\alpha,\beta}&=V_\alpha\cap V'_\beta=
\\
&=Q_1\cap \dots \cap Q_n\cap Q'_1\cap \dots \cap Q'_m.
\end{align*}
In particular $W_{\alpha,\beta}\in \s{P}$.

Finally note that
\[O\cap O'=\bigcup_{\alpha\in \c{I},\beta\in\c{J}}W_{\alpha,\beta}.\]
Hence $O\cap O'\in \s{O}$.
\qeds
